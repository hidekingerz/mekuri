# ファイル移動機能（D&D 仕分け）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ファイルリスト→フォルダツリーの D&D と、ビューワー内サブフォルダパネルへの D&D/クリックで、アーカイブ/PDF を別フォルダへ移動できるようにする。

**Architecture:** Rust に `move_file(src, dest_dir) -> Result<新パス, String>` コマンドを追加（衝突はエラー、クロスデバイスは copy+delete フォールバック）。フロントは HTML5 Drag & Drop（独自 MIME `application/x-mekuri-file`）で、メインは FileList→TreeNode、ビューワーは「パネル開時のみ draggable な見開き画像」→ SubfolderPanel チップ。移動後は `file-moved` イベントで FileList を再読込し、ビューワーは新パスで再読込して直前ページに復帰する。

**Tech Stack:** Tauri v2（Rust std::fs）/ React 19 + TypeScript / HTML5 DnD / cargo test + Vitest

**Spec:** `docs/superpowers/specs/2026-08-03-move-file-design.md`

## Global Constraints

- ブランチ: `feat/move-file`（`main` ベース）。`main` へ直接コミットしない
- 移動先に同名ファイルが存在する場合はエラー（上書き・自動リネームなし）
- ビューワーでの移動後はその場に留まり、直前のページ位置に復帰する
- 見開き画像のドラッグはサブフォルダパネルが開いている間のみ有効
- D&D の MIME タイプは `application/x-mekuri-file`（`src/utils/constants.ts` の `FILE_DRAG_MIME`）
- エラーメッセージは英語、コミットメッセージも英語
- 品質ゲート（Rust）: `cd src-tauri && cargo fmt && cargo clippy -- -D warnings && cargo test`
- 品質ゲート（フロント）: `pnpm format` / `pnpm lint` / `pnpm test` / `npx tsc --noEmit`

---

### Task 1: Rust `move_file` コマンド + API ラッパー

**Files:**
- Modify: `src-tauri/src/commands/fs.rs`（`trash_file` の直後にコマンド追加、`#[cfg(test)]` にテスト追加）
- Modify: `src-tauri/src/lib.rs`（`invoke_handler` に登録）
- Modify: `src/api/directory.ts`（`moveFile` ラッパー追加）
- Modify: `src/utils/constants.ts`（`FILE_DRAG_MIME` 追加）

**Interfaces:**
- Consumes: なし
- Produces（Task 2/3 が使用）:
  - Rust: `move_file(src: String, dest_dir: String) -> Result<String, String>`（Ok は移動後の新パス）
  - TS: `moveFile(src: string, destDir: string): Promise<string>`
  - TS: `export const FILE_DRAG_MIME = "application/x-mekuri-file";`

- [ ] **Step 1: 失敗するテストを書く**

`src-tauri/src/commands/fs.rs` の `#[cfg(test)] mod tests` 内（既存の trash_file テスト群の近く）に追加:

```rust
    #[test]
    fn move_file_success() {
        let dir = tempfile::tempdir().unwrap();
        let src = dir.path().join("a.zip");
        std::fs::write(&src, b"data").unwrap();
        let dest_dir = dir.path().join("sub");
        std::fs::create_dir(&dest_dir).unwrap();

        let result = move_file(
            src.to_string_lossy().to_string(),
            dest_dir.to_string_lossy().to_string(),
        );

        let new_path = result.unwrap();
        assert_eq!(new_path, dest_dir.join("a.zip").to_string_lossy());
        assert!(!src.exists());
        assert!(dest_dir.join("a.zip").exists());
        assert_eq!(std::fs::read(dest_dir.join("a.zip")).unwrap(), b"data");
    }

    #[test]
    fn move_file_collision_is_error_and_keeps_source() {
        let dir = tempfile::tempdir().unwrap();
        let src = dir.path().join("a.zip");
        std::fs::write(&src, b"data").unwrap();
        let dest_dir = dir.path().join("sub");
        std::fs::create_dir(&dest_dir).unwrap();
        std::fs::write(dest_dir.join("a.zip"), b"other").unwrap();

        let result = move_file(
            src.to_string_lossy().to_string(),
            dest_dir.to_string_lossy().to_string(),
        );

        assert!(result.unwrap_err().contains("already exists"));
        assert!(src.exists());
        assert_eq!(std::fs::read(dest_dir.join("a.zip")).unwrap(), b"other");
    }

    #[test]
    fn move_file_nonexistent_src() {
        let dir = tempfile::tempdir().unwrap();
        let result = move_file(
            "/tmp/nonexistent_mekuri_move_12345.zip".to_string(),
            dir.path().to_string_lossy().to_string(),
        );
        assert!(result.unwrap_err().contains("does not exist"));
    }

    #[test]
    fn move_file_src_is_directory() {
        let dir = tempfile::tempdir().unwrap();
        let sub = dir.path().join("sub");
        std::fs::create_dir(&sub).unwrap();
        let result = move_file(
            sub.to_string_lossy().to_string(),
            dir.path().to_string_lossy().to_string(),
        );
        assert!(result.unwrap_err().contains("not a file"));
    }

    #[test]
    fn move_file_dest_not_directory() {
        let dir = tempfile::tempdir().unwrap();
        let src = dir.path().join("a.zip");
        std::fs::write(&src, b"data").unwrap();
        let result = move_file(
            src.to_string_lossy().to_string(),
            dir.path().join("no_such_dir").to_string_lossy().to_string(),
        );
        assert!(result.unwrap_err().contains("not a directory"));
        assert!(src.exists());
    }

    #[test]
    fn move_file_same_directory_is_error() {
        let dir = tempfile::tempdir().unwrap();
        let src = dir.path().join("a.zip");
        std::fs::write(&src, b"data").unwrap();
        let result = move_file(
            src.to_string_lossy().to_string(),
            dir.path().to_string_lossy().to_string(),
        );
        assert!(result.unwrap_err().contains("same"));
        assert!(src.exists());
    }
```

- [ ] **Step 2: テストが失敗することを確認（RED）**

Run: `cd src-tauri && cargo test move_file`
Expected: コンパイルエラー（`move_file` が未定義）

- [ ] **Step 3: 実装**

`src-tauri/src/commands/fs.rs` の `trash_file` の直後に追加:

```rust
#[tauri::command]
pub fn move_file(src: String, dest_dir: String) -> Result<String, String> {
    let src_path = PathBuf::from(&src);
    if !src_path.exists() {
        return Err(format!("File does not exist: {src}"));
    }
    if !src_path.is_file() {
        return Err(format!("Path is not a file: {src}"));
    }
    let dest_dir_path = PathBuf::from(&dest_dir);
    if !dest_dir_path.is_dir() {
        return Err(format!("Destination is not a directory: {dest_dir}"));
    }

    // 同一フォルダへの移動は no-op ではなくエラーとして返す
    let src_parent = src_path
        .parent()
        .ok_or_else(|| format!("Invalid file path: {src}"))?;
    let same_dir = match (src_parent.canonicalize(), dest_dir_path.canonicalize()) {
        (Ok(a), Ok(b)) => a == b,
        _ => false,
    };
    if same_dir {
        return Err("Source and destination are the same folder".to_string());
    }

    let file_name = src_path
        .file_name()
        .ok_or_else(|| format!("Invalid file path: {src}"))?;
    let dest_path = dest_dir_path.join(file_name);
    if dest_path.exists() {
        return Err(format!(
            "A file with the same name already exists: {}",
            dest_path.display()
        ));
    }

    // rename を試し、失敗（クロスデバイス等）なら copy + remove にフォールバック。
    // コピー後の削除失敗は二重存在を許容し、データ喪失は起こさない。
    if std::fs::rename(&src_path, &dest_path).is_err() {
        std::fs::copy(&src_path, &dest_path).map_err(|e| format!("Failed to copy file: {e}"))?;
        std::fs::remove_file(&src_path).map_err(|e| {
            format!("Copied to destination but failed to remove the original: {e}")
        })?;
    }

    dest_path
        .to_str()
        .map(|s| s.to_string())
        .ok_or_else(|| "Destination path is not valid UTF-8".to_string())
}
```

`src-tauri/src/lib.rs` の `invoke_handler` の `commands::fs::search_directory,` の直後に追加:

```rust
            commands::fs::move_file,
```

- [ ] **Step 4: テストが通ることを確認（GREEN）**

Run: `cd src-tauri && cargo test move_file`
Expected: PASS（6 テスト）

Run: `cd src-tauri && cargo fmt && cargo clippy -- -D warnings && cargo test`
Expected: すべて成功

- [ ] **Step 5: フロントのラッパーと定数を追加**

`src/api/directory.ts` の `trashFile` の直後に追加:

```ts
export async function moveFile(src: string, destDir: string): Promise<string> {
  return invoke<string>("move_file", { src, destDir });
}
```

`src/utils/constants.ts` の末尾に追加:

```ts
/** D&D でファイルパスを受け渡すための独自 MIME タイプ */
export const FILE_DRAG_MIME = "application/x-mekuri-file";
```

Run: `npx tsc --noEmit && pnpm lint && pnpm test`
Expected: すべて成功

- [ ] **Step 6: フォーマット・コミット**

```bash
pnpm format
git add src-tauri/src/commands/fs.rs src-tauri/src/lib.rs src/api/directory.ts src/utils/constants.ts
git commit -m "Add move_file command with cross-device fallback"
```

---

### Task 2: メインウィンドウ D&D（FileList → FolderTree）

**Files:**
- Modify: `src/components/FileList/FileList.tsx`（draggable 化 + `file-moved` リスナー）
- Modify: `src/components/FolderTree/TreeNode.tsx`（drop ターゲット化）
- Modify: `src/components/FolderTree/FolderTree.tsx`（`onFileDrop` プロパティ中継）
- Modify: `src/App.tsx`（`handleFileDrop` + エラーバナー）
- Modify: `src/styles/global.css`（ハイライトとバナーのスタイル）

**Interfaces:**
- Consumes: Task 1 の `moveFile` / `FILE_DRAG_MIME`
- Produces: `TreeNode`/`FolderTree` の新プロパティ `onFileDrop: (srcPath: string, destDir: string) => void`

- [ ] **Step 1: FileList をドラッグ元にし、file-moved で再読込**

`src/components/FileList/FileList.tsx`:

1. import に追加: `import { FILE_DRAG_MIME } from "../../utils/constants";`
2. 「Reload file list when a file is trashed...」の useEffect を以下に置き換え:

```tsx
  // Reload file list when a file is trashed or moved from another surface
  useEffect(() => {
    if (!folderPath) return;
    const path = folderPath;
    const unlistenTrash = listen("file-trashed", () => {
      loadFiles(path);
    });
    const unlistenMove = listen("file-moved", () => {
      loadFiles(path);
    });
    return () => {
      unlistenTrash.then((fn) => fn());
      unlistenMove.then((fn) => fn());
    };
  }, [folderPath, loadFiles]);
```

3. ファイル項目の `<button>`（**検索結果の `resultFiles.map` と通常の `files.map` の両方**。フォルダ項目 `resultFolders` には付けない）に以下の属性を追加:

```tsx
            draggable
            onDragStart={(e) => {
              e.dataTransfer.setData(FILE_DRAG_MIME, file.path);
              e.dataTransfer.effectAllowed = "move";
            }}
```

- [ ] **Step 2: TreeNode を drop ターゲットにする**

`src/components/FolderTree/TreeNode.tsx` 全体を以下に置き換え:

```tsx
import { useState } from "react";
import type { TreeNodeData } from "../../types";
import { FILE_DRAG_MIME } from "../../utils/constants";
import { ChevronDown, ChevronRight, FolderIcon, FolderOpenIcon } from "../Icons/Icons";

type TreeNodeProps = {
  node: TreeNodeData;
  depth: number;
  selectedPath: string | null;
  onToggle: (path: string) => void;
  onSelect: (path: string) => void;
  onContextMenu: (e: React.MouseEvent, path: string) => void;
  onFileDrop: (srcPath: string, destDir: string) => void;
};

export function TreeNode({
  node,
  depth,
  selectedPath,
  onToggle,
  onSelect,
  onContextMenu,
  onFileDrop,
}: TreeNodeProps) {
  const { entry } = node;
  const isSelected = entry.path === selectedPath;
  const [isDragOver, setIsDragOver] = useState(false);
  // Hide chevron if the folder has no subfolders
  const hasChildren = entry.has_subfolders;

  const handleClick = () => {
    onSelect(entry.path);
  };

  const handleDoubleClick = () => {
    onToggle(entry.path);
  };

  const handleContextMenu = (e: React.MouseEvent) => {
    onContextMenu(e, entry.path);
  };

  const handleDragOver = (e: React.DragEvent) => {
    if (e.dataTransfer.types.includes(FILE_DRAG_MIME)) {
      e.preventDefault();
      e.dataTransfer.dropEffect = "move";
      setIsDragOver(true);
    }
  };

  const handleDragLeave = () => {
    setIsDragOver(false);
  };

  const handleDrop = (e: React.DragEvent) => {
    e.preventDefault();
    setIsDragOver(false);
    const srcPath = e.dataTransfer.getData(FILE_DRAG_MIME);
    if (srcPath) {
      onFileDrop(srcPath, entry.path);
    }
  };

  return (
    <>
      <div
        className={`tree-node tree-node--folder ${isSelected ? "tree-node--selected" : ""}${isDragOver ? " tree-node--drop-target" : ""}`}
        style={{ paddingLeft: `${depth * 20 + 8}px` }}
        onClick={handleClick}
        onDoubleClick={handleDoubleClick}
        onContextMenu={handleContextMenu}
        onDragOver={handleDragOver}
        onDragLeave={handleDragLeave}
        onDrop={handleDrop}
        onKeyDown={(e) => {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            handleClick();
          }
          if (e.key === "ArrowRight" && !node.isOpen) {
            e.preventDefault();
            onToggle(entry.path);
          }
          if (e.key === "ArrowLeft" && node.isOpen) {
            e.preventDefault();
            onToggle(entry.path);
          }
        }}
        role="treeitem"
        tabIndex={0}
      >
        {depth > 0 && (
          <div className="tree-node__guides">
            {Array.from({ length: depth }).map((_, i) => (
              <span
                key={`guide-${entry.path}-${i}`}
                className="tree-node__guide"
                style={{ left: `${i * 20 + 16}px` }}
              />
            ))}
          </div>
        )}
        {hasChildren ? (
          <button
            type="button"
            className="tree-node__chevron"
            onClick={(e) => {
              e.stopPropagation();
              onToggle(entry.path);
            }}
            tabIndex={-1}
          >
            {node.isOpen ? <ChevronDown size={14} /> : <ChevronRight size={14} />}
          </button>
        ) : (
          <span className="tree-node__chevron-placeholder" />
        )}
        <span className="tree-node__icon">
          {node.isOpen ? <FolderOpenIcon size={16} /> : <FolderIcon size={16} />}
        </span>
        <span className="tree-node__name">{entry.name}</span>
      </div>
      {node.isOpen &&
        node.children?.map((child) => (
          <TreeNode
            key={child.entry.path}
            node={child}
            depth={depth + 1}
            selectedPath={selectedPath}
            onToggle={onToggle}
            onSelect={onSelect}
            onContextMenu={onContextMenu}
            onFileDrop={onFileDrop}
          />
        ))}
    </>
  );
}
```

- [ ] **Step 3: FolderTree にプロパティを中継**

`src/components/FolderTree/FolderTree.tsx`:

1. `FolderTreeProps` に追加: `onFileDrop: (srcPath: string, destDir: string) => void;`
2. 関数引数の分割代入に `onFileDrop,` を追加
3. 末尾の `<TreeNode ...>` に `onFileDrop={onFileDrop}` を追加
（検索中のフラットリスト表示には付けない）

- [ ] **Step 4: App にハンドラとエラーバナーを追加**

`src/App.tsx`:

1. import に追加:

```tsx
import { moveFile, searchDirectory } from "./api/directory";
import { errorToString } from "./utils/errorToString";
```

（`searchDirectory` は既存 import の変更。`errorToString` は新規）

2. state 群に追加: `const [moveError, setMoveError] = useState<string | null>(null);`
3. `handleFavoriteAdded` の直後にハンドラを追加:

```tsx
  const handleFileDrop = useCallback(async (srcPath: string, destDir: string) => {
    setMoveError(null);
    try {
      await moveFile(srcPath, destDir);
      const { emit } = await import("@tauri-apps/api/event");
      await emit("file-moved");
    } catch (err) {
      setMoveError(errorToString(err));
    }
  }, []);
```

4. ツールバーの検索 input の直後にバナーを追加:

```tsx
        {moveError && (
          <div className="toolbar__error" role="alert">
            <span>Failed to move: {moveError}</span>
            <button
              type="button"
              className="toolbar__error-close"
              onClick={() => setMoveError(null)}
            >
              ×
            </button>
          </div>
        )}
```

5. `<FolderTree ...>` に `onFileDrop={handleFileDrop}` を追加

- [ ] **Step 5: スタイルを追加**

`src/styles/global.css` の末尾に追加:

```css
/* Drag & drop file move */
.tree-node--drop-target {
  outline: 2px solid #4a90d9;
  outline-offset: -2px;
  background-color: rgba(74, 144, 217, 0.15);
}

.toolbar__error {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-left: 12px;
  padding: 4px 10px;
  background: #5a2b2b;
  color: #f0c0c0;
  border: 1px solid #a05050;
  border-radius: 4px;
  font-size: 12px;
  min-width: 0;
}

.toolbar__error span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.toolbar__error-close {
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  font-size: 14px;
  padding: 0;
  line-height: 1;
}
```

- [ ] **Step 6: 検証**

```bash
npx tsc --noEmit && pnpm lint && pnpm test
```

Expected: すべて成功

手動確認（可能なら `pnpm tauri dev`）: ファイルをツリーのフォルダへ D&D → リストから消え、移動先選択で表示される。同名衝突でバナー表示。

- [ ] **Step 7: フォーマット・コミット**

```bash
pnpm format && pnpm lint
git add src/components/FileList/FileList.tsx src/components/FolderTree/TreeNode.tsx src/components/FolderTree/FolderTree.tsx src/App.tsx src/styles/global.css
git commit -m "Add drag-and-drop file move from file list to folder tree"
```

---

### Task 3: ビューワーのサブフォルダパネルと D&D 移動

**Files:**
- Create: `src/components/SubfolderPanel/SubfolderPanel.tsx`
- Modify: `src/components/Icons/Icons.tsx`（`MoveFolderIcon` 追加）
- Modify: `src/components/SpreadViewer/SpreadViewer.tsx`（`movePanel` / `initialPage` プロパティ、`currentPage` をハンドルに追加）
- Modify: `src/ViewerApp.tsx`（パネル state・移動ハンドラ・ページ位置復帰・エラーバナー）
- Modify: `src/styles/viewer.css`（パネル・バナーのスタイル）

**Interfaces:**
- Consumes: Task 1 の `moveFile` / `FILE_DRAG_MIME`
- Produces:
  - `SubfolderPanel({ archivePath: string; onMove: (destDir: string) => void })`
  - `SpreadViewerProps` 追加分: `initialPage?: number` / `movePanel?: { open: boolean; onToggle: () => void; dragData: string }`
  - `SpreadViewerHandle` に `currentPage: number` を追加

- [ ] **Step 1: MoveFolderIcon を追加**

`src/components/Icons/Icons.tsx` の `FitWindowIcon` の直後に追加（ビューワー用 CSS 変数に依存しない stroke アイコン）:

```tsx
export function MoveFolderIcon({ size = 16, className }: IconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 16 16"
      fill="none"
      className={className}
      aria-hidden="true"
    >
      <path
        d="M2 4C2 3.44772 2.44772 3 3 3H6.17157C6.43679 3 6.69114 3.10536 6.87868 3.29289L7.70711 4.12132C7.89464 4.30886 8.149 4.41421 8.41421 4.41421H13C13.5523 4.41421 14 4.86193 14 5.41421V12C14 12.5523 13.5523 13 13 13H3C2.44772 13 2 12.5523 2 12V4Z"
        stroke="currentColor"
        strokeWidth="1.2"
      />
      <path
        d="M6 8.5H10M10 8.5L8.5 7M10 8.5L8.5 10"
        stroke="currentColor"
        strokeWidth="1.2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
```

- [ ] **Step 2: SubfolderPanel を作成**

`src/components/SubfolderPanel/SubfolderPanel.tsx`:

```tsx
import { useEffect, useState } from "react";
import { getParentDirectory, readDirectoryFolders } from "../../api/directory";
import type { DirectoryEntry } from "../../types";
import { FILE_DRAG_MIME } from "../../utils/constants";
import { errorToString } from "../../utils/errorToString";

type SubfolderPanelProps = {
  /** 閲覧中ファイルのパス。この親フォルダ直下のサブフォルダを移動先候補にする */
  archivePath: string;
  onMove: (destDir: string) => void;
};

export function SubfolderPanel({ archivePath, onMove }: SubfolderPanelProps) {
  const [folders, setFolders] = useState<DirectoryEntry[]>([]);
  const [error, setError] = useState<string | null>(null);
  const [dragOverPath, setDragOverPath] = useState<string | null>(null);

  const parentDir = getParentDirectory(archivePath);

  useEffect(() => {
    let cancelled = false;
    readDirectoryFolders(parentDir)
      .then((entries) => {
        if (!cancelled) setFolders(entries);
      })
      .catch((err) => {
        if (!cancelled) setError(errorToString(err));
      });
    return () => {
      cancelled = true;
    };
  }, [parentDir]);

  return (
    <div className="subfolder-panel">
      {error ? (
        <p className="subfolder-panel__empty">Failed to load subfolders: {error}</p>
      ) : folders.length === 0 ? (
        <p className="subfolder-panel__empty">No subfolders in this folder</p>
      ) : (
        folders.map((folder) => (
          <button
            key={folder.path}
            type="button"
            className={`subfolder-panel__chip${dragOverPath === folder.path ? " subfolder-panel__chip--drag-over" : ""}`}
            title={folder.path}
            onClick={() => onMove(folder.path)}
            onDragOver={(e) => {
              if (e.dataTransfer.types.includes(FILE_DRAG_MIME)) {
                e.preventDefault();
                e.dataTransfer.dropEffect = "move";
                setDragOverPath(folder.path);
              }
            }}
            onDragLeave={() => setDragOverPath(null)}
            onDrop={(e) => {
              e.preventDefault();
              setDragOverPath(null);
              if (e.dataTransfer.getData(FILE_DRAG_MIME)) {
                onMove(folder.path);
              }
            }}
          >
            {folder.name}
          </button>
        ))
      )}
    </div>
  );
}
```

- [ ] **Step 3: SpreadViewer に movePanel / initialPage / currentPage を追加**

`src/components/SpreadViewer/SpreadViewer.tsx` に以下の変更を加える:

1. import の `Icons` に `MoveFolderIcon` を追加し、constants を import:

```tsx
import { FILE_DRAG_MIME } from "../../utils/constants";
import {
  FitWindowIcon,
  LtrIcon,
  MoveFolderIcon,
  RtlIcon,
  SinglePageIcon,
  SpreadViewIcon,
  TriplePageIcon,
} from "../Icons/Icons";
```

2. `SpreadViewerHandle` に `currentPage: number;` を追加:

```tsx
export type SpreadViewerHandle = {
  viewMode: ViewMode;
  readingDirection: ReadingDirection;
  currentPage: number;
  setViewMode: (mode: ViewMode) => void;
  toggleReadingDirection: () => void;
};
```

3. `SpreadViewerProps` に追加:

```tsx
  /** マウント時に開くページ index（移動後の位置復帰用） */
  initialPage?: number;
  /** ファイル移動パネル連携（指定時のみ移動ボタンとドラッグを有効化） */
  movePanel?: {
    open: boolean;
    onToggle: () => void;
    dragData: string;
  };
```

関数引数の分割代入に `initialPage, movePanel,` を追加。

4. `currentPage` の初期値を `initialPage` に:

```tsx
  const [currentPage, setCurrentPage] = useState(initialPage ?? 0);
```

5. `useImperativeHandle` のオブジェクトに `currentPage,` を追加:

```tsx
  useImperativeHandle(ref, () => ({
    viewMode,
    readingDirection,
    currentPage: currentPageFromGroup(currentGroup),
    setViewMode,
    toggleReadingDirection,
  }));
```

`currentPageFromGroup` を spreadLayout の import に追加する（`buildPageGroups, computeFitPageCount, currentPageFromGroup, groupIndexForPage`）。
※ state の `currentPage` ではなく `currentPageFromGroup(currentGroup)` を返すのは、グループ再構成後も「実際に表示中のグループ先頭ページ」を返すため。

6. `spread-viewer__pages` の div に draggable 関連を追加:

```tsx
      <div
        className={`spread-viewer__pages${isPair ? " spread-viewer__pages--pair" : ""}`}
        ref={pagesRef}
        onClick={handlePagesClick}
        draggable={movePanel?.open ?? false}
        onDragStart={(e) => {
          if (!movePanel?.open) {
            e.preventDefault();
            return;
          }
          e.dataTransfer.setData(FILE_DRAG_MIME, movePanel.dragData);
          e.dataTransfer.effectAllowed = "move";
        }}
      >
```

7. フッターの読み方向トグルボタンの直後（`spread-viewer__toggles` 内の最後）に追加:

```tsx
            {movePanel && (
              <button
                type="button"
                className={`spread-viewer__mode-toggle${movePanel.open ? " spread-viewer__mode-toggle--active" : ""}`}
                onClick={movePanel.onToggle}
                title="ファイルを移動"
                aria-pressed={movePanel.open}
              >
                <MoveFolderIcon size={16} />
              </button>
            )}
```

- [ ] **Step 4: ViewerApp を配線**

`src/ViewerApp.tsx` に以下の変更を加える:

1. import 変更:

```tsx
import { getSiblingArchives, moveFile, trashFile } from "./api/directory";
import { SubfolderPanel } from "./components/SubfolderPanel/SubfolderPanel";
```

2. state 追加（`trashError` の下）:

```tsx
  const [moveError, setMoveError] = useState<string | null>(null);
  const [movePanelOpen, setMovePanelOpen] = useState(false);
  const [resumePage, setResumePage] = useState(0);
```

3. 兄弟ナビゲーションと Trash 後遷移でページ位置をリセットするため、
   `useSiblingNavigation(archivePath, setArchivePath);` を以下に置き換え:

```tsx
  const navigateToArchive = useCallback((path: string) => {
    setResumePage(0);
    setArchivePath(path);
  }, []);

  useSiblingNavigation(archivePath, navigateToArchive);
```

   さらに `handleTrash` 内の `setArchivePath(nextPath);` を `navigateToArchive(nextPath);` に置き換え、
   `handleTrash` の useCallback 依存配列を `[navigateToArchive]` にする。

4. `handleTrash` の直後に移動ハンドラを追加:

```tsx
  const handleToggleMovePanel = useCallback(() => {
    setMovePanelOpen((v) => !v);
  }, []);

  const handleMoveTo = useCallback(async (destDir: string) => {
    const currentPath = archivePathRef.current;
    if (!currentPath) return;
    setMoveError(null);
    try {
      const newPath = await moveFile(currentPath, destDir);
      const { emit } = await import("@tauri-apps/api/event");
      await emit("file-moved");
      // 新パスで再読込しても同じページ位置から再開する
      setResumePage(spreadViewerRef.current?.currentPage ?? 0);
      setMovePanelOpen(false);
      setArchivePath(newPath);
    } catch (err) {
      setMoveError(errorToString(err));
    }
  }, []);
```

5. 最後の return を以下に置き換え（`key={archivePath}` で移動・遷移時の確実な再マウント、
   エラーバナーとパネルを追加）:

```tsx
  return (
    <div className="viewer">
      {moveError && (
        <div className="viewer__move-error" role="alert">
          <span>Failed to move file: {moveError}</span>
          <button type="button" onClick={() => setMoveError(null)}>
            ×
          </button>
        </div>
      )}
      <SpreadViewer
        key={archivePath}
        ref={spreadViewerRef}
        pageCount={pageCount}
        pageNames={pageNames}
        getPageDataUrl={getPageDataUrl}
        onSpreadChange={handleSpreadChange}
        onBack={!isPdf && archive.hasNestedCache ? archive.backToNestedList : undefined}
        defaultReadingDirection={defaultReadingDirection}
        initialPage={resumePage}
        movePanel={{
          open: movePanelOpen,
          onToggle: handleToggleMovePanel,
          dragData: archivePath,
        }}
      />
      {movePanelOpen && <SubfolderPanel archivePath={archivePath} onMove={handleMoveTo} />}
    </div>
  );
```

- [ ] **Step 5: viewer.css にスタイルを追加**

`src/styles/viewer.css`:

1. `.viewer` のルールに `position: relative;` を追加（オーバーレイの基準にする。既に
   ある場合は変更不要）
2. 末尾に追加:

```css
/* Subfolder move panel */
.subfolder-panel {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 52px;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  padding: 8px 12px;
  background: rgba(0, 0, 0, 0.75);
  z-index: 10;
}

.subfolder-panel__chip {
  padding: 4px 12px;
  border: 1px solid var(--viewer-color-border-button);
  border-radius: 12px;
  background: var(--viewer-color-bg-elevated);
  color: var(--viewer-color-text);
  cursor: pointer;
  font-size: 12px;
}

.subfolder-panel__chip:hover {
  background: var(--viewer-color-bg-hover);
}

.subfolder-panel__chip--drag-over {
  border-color: var(--viewer-color-accent-hover);
  background: var(--viewer-color-bg-hover);
}

.subfolder-panel__empty {
  color: var(--viewer-color-text-muted);
  font-size: 12px;
  padding: 4px;
}

/* Move error banner */
.viewer__move-error {
  position: absolute;
  top: 8px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 12px;
  border-radius: 4px;
  background: #5a2b2b;
  color: #f0c0c0;
  border: 1px solid #a05050;
  font-size: 12px;
  z-index: 20;
}

.viewer__move-error button {
  background: none;
  border: none;
  color: inherit;
  cursor: pointer;
  font-size: 14px;
  padding: 0;
  line-height: 1;
}
```

- [ ] **Step 6: 全品質ゲート**

```bash
npx tsc --noEmit && pnpm lint && pnpm test
cd src-tauri && cargo fmt && cargo clippy -- -D warnings && cargo test
```

Expected: すべて成功

- [ ] **Step 7: フォーマット・コミット**

```bash
pnpm format && pnpm lint
git add src/components/SubfolderPanel/SubfolderPanel.tsx src/components/Icons/Icons.tsx src/components/SpreadViewer/SpreadViewer.tsx src/ViewerApp.tsx src/styles/viewer.css
git commit -m "Add subfolder move panel with drag-and-drop in viewer"
```

---

## 手動確認（マージ前）

`pnpm tauri dev` で:

1. ファイルリスト → ツリーのフォルダへ D&D で移動。リストが再読込される
2. 同名衝突: ツールバーにエラーバナー、ファイルは移動されない
3. ビューワー: 📁（移動）ボタンでパネル開閉。開いた状態で画像をチップへ D&D →
   移動後も同じページ位置で閲覧継続。メインのファイルリストも更新される
4. チップのクリックでも移動できる
5. パネルが閉じていると画像はドラッグできず、クリックページ送りが従来どおり
6. サブフォルダの無いフォルダで「No subfolders in this folder」表示
7. 移動エラー（衝突）はビューワー上部のバナーに表示され × で閉じられる
8. Trash・兄弟ナビゲーションが従来どおり動く（位置リセットも正常）

## 完了後

1. `git push -u origin feat/move-file`
2. `gh pr create` で `main` 向け PR を作成
3. CI 通過後にマージ
