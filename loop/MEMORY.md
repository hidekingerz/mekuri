# MEMORY — ループの記憶（背骨）

> 毎周このファイルを読み、末尾に追記する。会話履歴ではなく**このファイルが記憶**。
> 人間が消さない限り、後の周でも過去の試行を踏まえて動ける。

## Done（達成済み）

- [m8b:pushhub] **マイルストーン8b の基盤＝main→webview の push チャネル（SSE）を純粋・テスト可能な `desktop/pushHub.ts` として新設**（VISION M8b の「`Deno.serve` に SSE/long-poll を足し webview が購読」の第一段）。真因（Open [operator/m8] 8b）＝menu クリック結果や窓間イベント delivery を main→webview へ届ける push が `executeJs`/`win.bind` と同じく**採用窓に届かない**こと。HTTP/SSE は窓非依存なので確実に届く（`/__invoke` の webview→main と対になる逆方向経路）。**実装**: `PushHub` クラス＝`subscribe(windowLabel, sink)→解除関数`（購読者を `Set` 管理、`[...set]` スナップショットで反復＝配信中の解除に耐える＝event.ts と同方針）／`broadcast(msg)`（全窓＝窓間イベント用）／`sendTo(windowLabel, msg)`（特定窓＝menu クリック結果用）／`subscriberCount` getter。`formatSse(msg)`＝`data: <json>\n\n`。`handleEventsRequest(req, hub)`＝`GET /__events?windowLabel=<label>` を処理し、`windowLabel` 欠落/空は 400、`ReadableStream` の `start` で `hub.subscribe`（sink は SSE フレームを `controller.enqueue`）・`cancel` で解除し、`text/event-stream`+`cache-control:no-cache` の `Response` を返す純粋関数。チャネル定数 `EVENT_CHANNEL="event"`/`MENU_CLICK_CHANNEL="menuClick"` と `EVENTS_PATH="/__events"`/型 `PushMessage`/`PushSink` も export。`desktop/mod.ts` から re-export（型検査対象に含める）。**テスト**: `pushHub.test.ts`（8）＝broadcast 全配信／sendTo は label 一致のみ／unsubscribe で配送停止＋count／formatSse 形式／windowLabel 欠落 400／空 400／SSE ストリームが broadcast を受信し cancel で購読解除（count→0）／sendTo がターゲット窓ストリームのみへ。**検証**: `deno task verify` **231 passed**（223→231、fmt/lint/check（canary main.ts 含む）/test 全通過）。**設計判断**: (1) 今周は**基盤＋テストのみ**＝main.ts 配線・event/menu の移行・`executeJs` push 廃止は次周（1周1タスク・最小変更）。(2) Desktop API 非依存（`ReadableStream`/`Request`/`Response` は web 標準）で `deno test` 完結。(3) `cancel` で必ず unsubscribe＝窓クローズ/切断でリーク無し。テストは `reader.cancel()` を await して count→0 を担保。**落とし穴/学び**: (1) 当初 `PATHS`（INVOKE_PATH 参照）を export していたが過剰設計＝削除し import も除去。(2) `res.body` は `assertExists` で絞ってから `getReader()`（非 null 断定回避）。(3) `deno fmt` 必須（mod.ts の export 並べ替え・複数行リテラル整形）。commit はこの ITERATE で。**残（M8b／LOOP_DONE ゲート）**: ①main.ts で `PushHub` を生成し `Deno.serve` に `GET /__events` を配線、②`handleEventCommand` の窓間イベント delivery を `executeJs` push → `hub.broadcast({channel:EVENT_CHANNEL,...})` へ、③menu クリック配送を `executeJs` push → `hub.sendTo(windowLabel,{channel:MENU_CLICK_CHANNEL,...})` へ（`contextMenuClick` の窓 label 解決が要る）、④`menu_` を HTTP transport へ移行し `frontend/invoke.ts` の `BINDINGS_SCOPED_PREFIXES` を空に、⑤`frontend` に EventSource 購読 shim を置き `__mekuriDeliverEvent`/`__mekuriMenuClick` hook へ配送。LOOP_DONE は `verify`＋`check-no-tauri`＋`pnpm test`＋`smoke`＋ M8 回帰テスト全緑から。

- [m8a:cache] **マイルストーン8a＝書庫バイト列の path 別キャッシュを導入し、画像1枚ごとの「書庫全体 `Deno.readFile`＋全再パース」を撤廃**（VISION M8a／Open [operator/m8] の最優先）。真因（Open [operator/m8] 記載）＝`getImageBase64` が画像取得のたびに `openZip`/`openRar` 経由で `Deno.readFile(archivePath)` を呼び書庫全体を毎回再読込していたこと（Rust 版 `ZipArchive::by_name` は中央ディレクトリ＋該当エントリのみストリーム読み＝軽い）。**実装**: ①`deno-app/backend/archive/cache.ts`（新設・Desktop 非依存の純粋ロジック）＝`readArchiveBytes(path, reader=Deno.readFile)` が path 別 LRU（`MAX_ENTRIES=2`、最近使用を末尾へ、超過は先頭を退避）で書庫バイト列を共有。`clearArchiveCache()`（テスト分離用）と型 `ArchiveReader` も export。**重要設計**: キャッシュ値は `Uint8Array` ではなく **`Promise<Uint8Array>`** を保持＝並行要求が in-flight の読み込みを共有でき、書庫あたり reader 1 回に収まる（resolve 済みバイト列を載せる素朴案だと、最初の `await reader()` 解決前に来る並行呼びが全てキャッシュミスして複数回 readFile してしまう）。読み込み失敗時は `.catch` でキャッシュから自エントリを除去し後続要求が読み直せるようにする。②`zip.ts` の `openZip`・`rar.ts` の `openRar` の `Deno.readFile(archivePath)` を `readArchiveBytes(archivePath)` へ差し替え（`listImages`/`analyzeContents`/`getImageBase64`/`extractNestedArchive` が全て openZip/openRar 経由なのでキャッシュ共有される）。③`archive/mod.ts`→`backend/mod.ts` から `readArchiveBytes`/`clearArchiveCache`/`ArchiveReader` を re-export（型検査対象に含める）。**テスト**: `cache.test.ts`（5）＝同一 path 連続で reader 1 回／別 path はそれぞれ 1 回／LRU 2 件超過で最古退避／ヒットで最近使用へ移動（b 退避され a は残る）／clear 後は再読込。`zip.test.ts` に統合回帰 2＝**連続 `getImageBase64` 3 回で対象書庫の `Deno.readFile` が 1 回**（`Deno.readFile` をスパイ差し替え、対象 path のみカウント）／**並行 `getImageBase64` 4 回が全成功し書庫読込 1 回**（`Entry not found`/失敗ゼロを担保）。**検証**: `deno task verify` **223 passed**（216→223、fmt/lint/check（canary main.ts 含む）/test 全通過）。**落とし穴/学び**: (1) 並行 dedupe には Promise キャッシュが必須（バイト列キャッシュでは並行ミスする）。(2) `readArchiveBytes` は `async` でなく Promise を直返し（in-flight Promise をそのまま共有するため。lint require-await にも触れない）。(3) スパイは `Deno.readFile = ((p,opts)=>...) as typeof Deno.readFile` で差し替え、`finally` で必ず原状回復＋`clearArchiveCache()`（テスト間でグローバルキャッシュが漏れないように。各 cache テストも先頭で clear）。(4) キャッシュは file mtime を見ない＝書庫が閲覧中に書き換わると stale だが、本アプリの書庫は静的で LRU=2 で別書庫を開けば退避されるためスコープ内で許容。commit はこの ITERATE で。**残（M8b／LOOP_DONE ゲート）**: 8b＝ビューワー右クリックメニュー（`menu_` のみ bindings transport で不達。main→webview の push チャネル＝SSE/long-poll を `Deno.serve` に足し、menu クリック配送と窓間イベント delivery をそこへ移す）。LOOP_DONE は `verify`＋`check-no-tauri`＋`pnpm test`＋`smoke`＋ M8 回帰テストが全緑から。

- [m7:http-window] **[m7:http-transport-step1] の次段＝`window_*` 系統を bindings transport から HTTP transport へ移行（1周1系統の window）。** 白画面の主因は起動時 `src/App.tsx` が呼ぶ `window_set_size`/`window_show`/`window_set_title` が `win.bind` 未達で reject すること（[m7:whitescreen-rootcause] でも smoke の reject×3 はこれら window 系が中心）。HTTP は表示窓へ確実に届くため window 系も HTTP へ移すが、HTTP では「どの窓からの呼びか」が消えるので、**webview が自窓 label をリクエストに載せ、main 側が label→窓を解決する**設計（[m7:denidian-http-pivot]/Open の (B) 設計案を実装）。**実装5層**: ①`desktop/httpInvoke.ts` の `InvokeDispatch` を `(command, args, windowLabel?)` に拡張し、`POST /__invoke` body から `windowLabel`（任意・string 検証、非 string は 400）を取り出して dispatch へ渡す。②`desktop/windowRegistry.ts`（新設・純粋）＝`handleWindowCommandByLabel(resolve, windowLabel, command, args)`＝label 欠落/空はエラー、`resolve(label)` で窓が引けねばエラー、引ければ `handleWindowCommand(win, ...)` へ委譲。③`frontend/windowLabel.ts`（新設）＝`windowLabelFromLocation({search})`（`archive` クエリ有→`viewer-${hashCode(archive)}`、無→`MAIN_WINDOW_LABEL="main"`）と `currentWindowLabel()`（`globalThis.location` から算出、無い環境は "main"）。**hashCode は `desktop/viewer.ts` と同一アルゴリズムを複製**（frontend を browser-safe に保つため import せず複製）し、`windowLabel.test.ts` で `viewerLabel`（desktop）と値一致を固定して divergence を防ぐ。④`frontend/invoke.ts`＝transport 振り分けを反転＝`event_`/`menu_` のみ bindings（`BINDINGS_SCOPED_PREFIXES`）、`window_` 含むそれ以外は HTTP。`invokeViaHttp` の body に `windowLabel` を追加、`invoke()` に第6引数 `windowLabel: string = currentWindowLabel()` を追加（`src/api/*.ts` は2引数のまま）。⑤`main.ts`＝`resolveWindow(label)`（main→`win`、`viewer-*`→`viewerWindows`、閉窓は削除して undefined）を追加し、HTTP dispatch の `handleInvoke` に第4引数 `windowFn=(cmd,args)=>handleWindowCommandByLabel(resolveWindow, windowLabel, cmd, args)` を渡す。`bindInvoke`（bindings 経路）は event/menu のため温存（window_ 経路は今後 dead だが無害）。**検証**: `deno task verify` **216 passed**（httpInvoke.test.ts に windowLabel 転送/非 string 400 を 2 追加、windowRegistry.test.ts 6 新設、windowLabel.test.ts 4 新設、invoke.test.ts の旧 window_ bindings テストを event_/menu_ へ書換＋window_ HTTP テスト追加。fmt/lint/check(canary main.ts 含む)/test 全通過）。フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint` クリーン・`pnpm build` 成功（windowLabel.ts が browser-safe で bundle 解決）・`check-no-tauri` OK（0件・src/ 無改変）。**設計判断**: label を URL から算出する案を採用＝viewer の URL（`viewer.html?archive=...`）は既に archive を持ち、main 側 registry キー（`viewerLabel(archivePath)`）と同じ hashCode で一致する。main 窓は archive 無し→"main"。これでメイン窓の navigate URL に label を埋め込む工夫（auto-navigation で困難）が不要。**落とし穴/学び**: (1) `InvokeDispatch` の引数を増やしたが、`main.ts`/テストの 2 引数ラムダは TS 的に代入可（少ない arity は OK）。(2) frontend↔desktop の hashCode 複製は test で値一致を pin（`windowLabel.test.ts` が `../desktop/viewer.ts` の `viewerLabel` を import して比較）＝テストは Deno 実行で browser bundle に入らないので安全。(3) `deno fmt` 必須（複数行呼び出しが整形対象）。**次周タスク（最優先・1タスク）**: (A) **要 operator/canary**: `deno task smoke` で起動時 window 系 reject が消えたか観測（残る reject は event/menu 由来のはず＝ViewerApp の menu や FileList の event listen 設置時）。(B) 残る **event→menu を同方式で HTTP 化**。event は窓間 pub/sub なので `handleEventCommand` は全窓ブロードキャストで label 不要（emit 元の窓特定は不要）＝HTTP 化は素直。menu は `handleMenuCommand(win,...)` が窓固有なので window と同じく windowLabel→窓解決が要る（`handleWindowCommandByLabel` と同型の menu 版 or 汎用化）。これらが済めば `win.bind("invoke")` を撤廃でき smoke 緑＝白画面解消で LOOP_DONE ゲートへ。

- [m7:http-transport-step1] **[m7:denidian-http-pivot] の最優先タスク＝invoke transport を `win.bind`→HTTP へ切り替える第一歩を実装。窓非依存の data/store 系コマンドを `fetch("/__invoke")` 経由（Deno.serve エンドポイント）へ振り分け、窓固有の window/event/menu 系は `bindings.invoke` のまま残した。** 白画面の真因（[m7:whitescreen-rootcause] 計測確定＝`win.bind` が framework の表示窓に届かない）を、リファレンス denidian と同じ HTTP transport で回避する第一段。HTTP は窓に依存しないため表示窓へ確実に届く。**実装3層**: ①`deno-app/desktop/httpInvoke.ts`（純粋・テスト可能）新設＝`handleInvokeRequest(req, dispatch)` が `POST /__invoke` の body `{command, args}` を検証し dispatch へ委譲、`{ok:true, value}`(200)／`{ok:false, error}`(400 検証失敗・500 dispatch 例外) を JSON で返す。`INVOKE_PATH="/__invoke"` 定数も export。Request/Response は web 標準で `deno test` で完結（Desktop API 非依存）。②`main.ts` の `Deno.serve` ハンドラに `if (POST && /__invoke) return handleInvokeRequest(req, (cmd,args)=>handleInvoke([cmd,args], undefined, storeFn))` を追加（**data + store のみ**＝window/event/menu の `*Fn` は渡さない＝HTTP では窓を特定できないため）。③`frontend/invoke.ts` を2 transport 化＝`isWindowScoped(command)`（接頭辞 `window_`/`event_`/`menu_`）なら従来の `invokeViaBindings`（リトライ付き）、それ以外は新 `invokeViaHttp`（`fetch(INVOKE_PATH, POST {command, args:args??null})` → `{ok,value|error}` を読み、非2xx/ok:false は error を throw、成功は value 返却）。`invoke()` に第5引数 `fetchFn: FetchFn`（既定 `globalThis.fetch`、テスト注入用）を追加（`src/api/*.ts` は2引数のまま）。`desktop/mod.ts` から httpInvoke を re-export。**検証**: `deno task verify` **203 passed**（httpInvoke.test.ts 7 + invoke.test.ts を HTTP/bindings 両 transport へ再編＝計9、fmt/lint/check(canary main.ts 含む)/test 全通過）。**設計判断**: HTTP path はリトライ無し（server は framework の auto-navigation が readiness を待ってから表示するので接続済み前提）。bindings path のリトライ（`No callback bound` 吸収）は window/event/menu 用に温存。**落とし穴/学び**: invoke.test.ts の旧テストは data コマンド（read_directory 等）を bindings mock で検証していたが、それらは HTTP path に移ったので mockFetch ベースへ書き換え。retry/不在テストは `window_*` コマンドへ変更（bindings path に残るため）。`src/api/invoke.ts` 経由で `src/` も自動的に HTTP transport になる（invoke 再 export のみ、呼び出し側無改変）。**次周タスク（最優先・1タスク）**: smoke で data 系の `No callback bound` が消えたか観測（要 canary・GUI なのでループ内自動検証は不可＝operator 確認領域）。その後、残る **window/event/menu を HTTP 化**するには「どの窓からの呼びか」をリクエストに載せる設計が要る（候補: 各窓の navigate URL に窓 label を埋め、frontend が `fetch` 時に label を body/header へ付け、main 側 dispatch が label→窓を引いて handleWindowCommand/handleEventCommand/handleMenuCommand へ委譲）。これが完了すれば `win.bind` を完全撤廃でき白画面が解消するはず。1周1系統（window→event→menu）を目安。

- [m7:denidian-http-pivot] **公式リファレンス実装 denidian を精読し、白画面の解決方向を決定づける発見をした＝「`deno desktop` の webview→backend 通信は `win.bind`/`bindings.invoke` ではなく `Deno.serve` の HTTP エンドポイントへの `fetch` で行うのが正攻法」。今周の最小変更（main.ts を denidian パターンへ寄せる＝メイン窓の明示 `win.navigate(origin)`/`win.show()` を削除）は verify green でコミットしたが、smoke は依然 FAIL（同一＝`No callback bound for: invoke`×3、回帰なし）。** 経緯: [m7:whitescreen-rootcause-followup] の最有力策（a）＝「framework の auto-navigation に委ねる」を doc 調査で具体化。`docs.deno.com/runtime/desktop/{windows,bindings}` と Deno 2.9 blog を精読 →「`Deno.serve` が初期ウィンドウを自動オープン＆自動 navigate」「**最初の `new Deno.BrowserWindow()` が初期窓を採用**」「bindings は per-window」を確認。リファレンス app **denidian**（`github.com/bartlomieju/denidian`、docs が指す唯一の完全実装）の `main.ts` を**逐語取得**して判明した決定的事実: ①window は `Deno.serve` の**前**に構築（採用）、②**明示 navigate/show は無し**（framework が server readiness 時に自動表示）、③**`win.bind` を一切使っていない**＝webview は `fetch("/api/notes")` 等で `Deno.serve` ハンドラを直接叩く HTTP 通信。`win` で使うのは `setSize`/`setTitle`/`setApplicationMenu`/`addEventListener("menuclick")`/`executeJs`（main→webview の push）のみ。**結論**: 我々の白画面真因＝「`win.bind("invoke",...)` が表示窓に届かない」は、この canary の採用バグ（[m7:whitescreen-rootcause] で計測確定）に加え、そもそも**リファレンスが bind を使わず HTTP で解決している**＝`win.bind` 依存の IPC 設計自体を HTTP transport へ切り替えるのが正解。今周は①②を main.ts に反映（denidian と同型に）したが、bind に依存する限り smoke は通らない（実証＝navigate/show を消しても reject×3 不変。`[web]` 行が出る＝表示窓は我々の served content を確かに読み込み ERR_FORWARDER も動いている＝表示窓＝served window で確定、ただし未 bind）。**検証**: `deno task verify` **194 passed**（fmt/lint/check(canary main.ts 含む)/test 全通過、テスト数不変＝ロジック移植なし）。`deno task smoke` は FAIL（reject×3、回帰なし）。**変更内容**: `main.ts` 末尾の `win.navigate(\`${origin}/\`)` と `win.show()`（メイン窓のみ）を削除し、auto-navigation 委譲の説明コメントへ置換。`origin` はビューワー窓（明示生成＝別窓、navigate/show 継続）の open_viewer 内参照で引き続き使用。viewer 窓は denidian と異なり「2個目以降の構築＝新規窓」なので明示 navigate/show が正しい（不変）。**落とし穴/学び**: denidian は per-window bind が機能しても**使っていない**ので「bind が効くか」は denidian からは判らない。我々の計測（[m7:whitescreen-rootcause]）が「この canary では表示窓に bind が届かない」を示しており、HTTP transport へ移れば窓の採用問題を完全に回避できる（HTTP は窓非依存）。**次周タスク（最優先・1タスク・大きめなので最小スコープで）**: invoke transport を `win.bind`→HTTP へ切り替える第一歩。具体案: `main.ts` の `Deno.serve` ハンドラに `POST /__invoke`（body=`{command,args}`）を追加し `handleInvoke(...)`（store/window/event/menu 分岐込み）へ委譲して JSON で返す → `deno-app/frontend/invoke.ts` の `invoke` を `globalThis.bindings.invoke` から `fetch("/__invoke",{method:"POST",body:JSON.stringify({command,args})})` へ差し替え（テストは fetch 注入でカバー、既存 retry ロジックは HTTP エラー時に転用 or 撤去を検討）。**ただし window/event/menu は「どの窓からの呼びか」を HTTP では特定しにくい**（bind は窓を閉包できた）ので、まず**窓非依存の data 系（read_directory/archive/store_*）だけ**を HTTP 化し smoke で `No callback bound` が減るか観測→段階移行するのが安全（1周1系統）。window/event/menu は窓識別子をリクエストに載せる等の追加設計が要る（別周）。`frontend/invoke.ts` の bind 準備待ち retry は HTTP 化後は不要になる可能性。

- [m7:bind-before-serve] **構築順仮説（採用は同期構築でのみ成立＝`await Store.load()` 前に窓を作れば直る）を実装して検証 → 白画面は**解消せず**、レース仮説を否定。表示窓≠bind 済み win の別窓問題を決定的に裏付けた。** docs（runtime/desktop ライフサイクル・windows・bindings）を精読して得た仮説: 「`deno desktop` は起動時に初期ウィンドウを自動オープンし `Deno.serve` ハンドラへ**自動遷移**させる」「最初の `new Deno.BrowserWindow()` がその初期窓を**採用**する」。現行 main.ts は `await Store.load()` の**後**に窓を構築していたため、トップレベル await で制御が戻る間に初期窓が自動遷移して React が `invoke` を呼ぶ→未 bind→`No callback bound` になる、採用は初回スクリプト評価の同期実行中の構築でのみ成立する、という読み。**変更**: `main.ts` を再構成し、`const win = new Deno.BrowserWindow(...)` と `bindInvoke(win)` を **`Deno.serve` も `await Store.load()` も走らせる前の最初の同期処理**に移動。store は遅延ロード化（`let storePromise; loadStore()=storePromise??=Store.load(settingsPath(),{autoSave:true})`、bindInvoke の storeFn と open_viewer の loadViewerSettings から `await loadStore()` で参照。`OpenViewerDeps.loadViewerSettings` は元から `Promise` 戻り対応なので async 化で型 OK）。serve/origin/open_viewer/navigate/show はその後ろへ。**検証**: `deno task verify` **194 passed**（fmt/lint/`deno check main.ts`(canary 型)/test 全通過、テスト数不変＝ロジック移植なし）。**しかし `deno task smoke` は依然 FAIL**＝`[web] reject: Error: No callback bound for: invoke | @user-script:1:58:33` が**まだ3回**。前周追加の frontend リトライ（`No callback bound` で 20ms×最大50≒1s 再試行）が効いていれば bind が後から付く窓では救えるはずだが、3 reject のまま＝**表示窓には最後まで bind が来ない**＝[m7:whitescreen-rootcause] の「表示 webview と `new Deno.BrowserWindow()` の `win` は別オブジェクト」を裏付け、構築タイミングのレースは**真因ではない**ことを確定。**この変更自体は docs 準拠で more-correct（bind を auto-navigation より前に同期登録）かつ無害なので baseline として残す（VERIFY green でコミット）が、白画面は未解消。** **落とし穴/学び**: 「採用は同期構築でのみ成立」説は誤り（同期最初に作っても別窓のまま）。引数なし `new Deno.BrowserWindow()` でも不変（[m7:whitescreen-rootcause] 既出）と合わせ、**コンストラクタ呼び出しでは表示中の初期窓を掴めない**と結論。**次周タスク（最優先・1タスク）**: 「表示窓のハンドルを得て bind する」別経路を探す。鋭い新仮説＝**compiled `deno desktop` アプリでは、表示窓は main.ts の `new Deno.BrowserWindow()` とは独立に runtime が auto-serve/auto-open している**（だから別窓・未 bind）。次の最小実験案: (a) main.ts から `Deno.serve` と明示 `win.navigate`/`win.show` を**外し**、`--include ../dist` を runtime の framework auto-detect（dist=静的サイト）に委ねて、`new Deno.BrowserWindow()`+bindInvoke のみで bind が webview に届くか smoke で確認（届けば「自前 serve+navigate が別窓を生んでいた」が確定）。(b) bind ハンドラ先頭に `console.error("[main] invoke hit")` を一時計測し、表示窓からの呼びが**どの**窓ハンドラに着くか観測（[m7:whitescreen-rootcause] の計測再開）。(c) `https://docs.deno.com/go/desktop` と GitHub `denoland/deno` の desktop サンプル（PR #33441 / examples）で **bind を使う公式サンプルの起動形**（serve するか・navigate するか・窓を作るか）を確認。**`Deno.serve` を main.ts が持つべきか否か**が核心。

- [m7:whitescreen-rootcause] **白画面の真因を計測で確定（最重要）＋フロント invoke に bind 準備待ちリトライ（operator hint③ の安全網）を追加**。前周 [m7:smoke-instrument]/[operator/smoke-msg] の `Error: No callback bound for: invoke` ×3 を起点に、`main.ts` を一時計測（`console.error` を bind ハンドラと bind 直後に、`mekuriPing` 別名 bind、注入 PROBE スクリプト、`win.executeJs("globalThis.__winMarker='WIN'")`）して**グラウンドトゥルース**を取得。結果（決定的）: ①`[main] bindInvoke(win) done` は出るが `[main] invoke handler called:` は**一度も出ない**＝bind は成功しても webview からそのハンドラに到達していない。②PROBE（表示ページで実行）から `bindings.invoke` も `bindings.mekuriPing` も**両方** `No callback bound` で失敗＝特定コマンドや「invoke が予約名」の問題**ではない**（全 bind が不達）。③`probe: winMarker=undefined`＝`win.executeJs` が `win` のコンテキストに付けたマーカーを**表示ページが見ない**＝**表示 webview と `new Deno.BrowserWindow()` で得た `win` は別オブジェクト**。**確定した真因**: この canary では `new Deno.BrowserWindow()` は起動時に自動オープンされる初期ウィンドウを**採用せず別ウィンドウを生成**している（公式 docs の「first construct adopts the initial window」と実挙動が食い違う）。表示・クラッシュするのは bind 皆無の**初期ウィンドウ**で、全 bind 済みの `win` は別の（背面/非表示）窓。だから window/store/event/menu/open_viewer 全 IPC が webview から不達＝起動時 `No callback bound` 連発＝白画面。**ルールアウト済み（いずれも smoke 不変）**: (a) `await Store.load` の bind 前 await レース（store を遅延ロード化しても不変）、(b) コンストラクタ options 有無による採用差（引数なし `new Deno.BrowserWindow()` でも不変）、(c) navigate/reload のタイミング（明示 navigate 削除・`win.reload()` 強制とも不変）、(d) "invoke" 予約名衝突（mekuriPing も不達）。**今周コミットする変更**: 計測は全て破棄し `main.ts` はクリーンに戻した（`git checkout main.ts`）。残すのは `deno-app/frontend/invoke.ts` の **bind 準備待ちリトライ**のみ＝`No callback bound`（＝未登録シグナル）で reject した場合だけ短間隔（20ms×最大50≒1s）で再試行、それ以外（backend 実エラー・bindings 不在）は即透過。bind は一度登録すれば外れず、`No callback bound` の間はコマンド未実行＝安全に再試行可。`Delay` 注入でテスト可能（既定 `setTimeout`）。`invoke.test.ts` に2件（未登録→登録後に成功で待機回数検証／実エラーは即伝播・待機ゼロ）。**注意**: このリトライは真因（別窓問題）を**直さない**（表示窓には永遠に bind が来ないのでリトライは枯渇するだけ）。次周で別窓問題を直せば、残る起動レースの安全網として機能する＝前方互換の地ならし。operator が hint③ として推奨した策。**検証**: `deno task verify` 194 passed（invoke.test.ts 192→194）。fmt/lint/check(canary main.ts 含む)全通過。smoke は**まだ FAIL**（真因未修正のため当然）。**次周タスク（最優先・1タスク）**: **実際に表示される初期ウィンドウを bind する方法**を見つける。要調査: Deno Desktop API に初期/現在ウィンドウへのアクセス手段があるか（静的 `Deno.BrowserWindow.getCurrent`/`getAll`、ウィンドウ生成イベント、または正しい採用パターン）。候補: (a) 自動オープンを無効化し自前で唯一の窓を作って navigate、(b) ウィンドウ生成/ready イベントを listen してそこで bind、(c) docs/bindings ページ・PR #33441 の実例で採用の正しい書き方を確認。`https://docs.deno.com/runtime/desktop/windows/` と `/bindings/` を精読。

- [m7:smoke-instrument] **[m7:final-gate]②＝smoke を初実行し、白画面が**まだ解消していない**ことを検知。FAIL の原因特定に向けて ERR_FORWARDER を強化（エラー詳細を出すよう純粋関数化＋テスト）した**。`cd deno-app && deno task smoke`（canary・PATH 先頭 `~/.deno-canary/bin`）を実行: pnpm build → desktop build まで成功するが、ヘッドレス起動で `[web] reject: @user-script:1:58:33` が**3回**出て FAIL（`__TAURI_INTERNALS__` ではなく、起動時に走る複数の `bindings.invoke` 系 Promise が unhandled reject している疑い）。**起動シーケンス調査**: `src/App.tsx` の loadSettings useEffect が `getWindowSettings()`(store_get)→`win.setSize()`(window_set_size)→`win.show()`(window_show、finally 内・try 無し)、別 useEffect が `getCurrentWindow().setTitle()`(window_set_title、try 無し)を invoke。3回同一 reason＝`frontend/invoke.ts` の同一 `globalThis.bindings.invoke` 行を通る複数 invoke が全 reject している可能性が高い（bind ハンドラの throw か bindings.invoke 自体の不調）。**だが旧 ERR_FORWARDER は `(reason.stack)||reason` で stack 優先のため、WebKit 系 webview で stack に message が含まれず `@user-script:位置` だけになり原因コマンドを特定できなかった**。→ **対策（今周の変更）**: `deno-app/desktop/errorForwarder.ts` を新設し、`errorForwarderScript()` で reason を `name+message+stack` の順に連結して出す `D(r)` ヘルパを埋め込む純粋関数化（`error` も `e.error` を D で整形）。`desktop/errorForwarder.test.ts`(5)＝script タグ/__log/両 addEventListener/reject: prefix/name・message・stack 連結を検証。`desktop/mod.ts` から re-export、`main.ts` の `ERR_FORWARDER` 定数を `errorForwarderScript()` 呼び出しへ置換（インライン文字列を撤去）。**検証**: `deno task verify` 192 passed（errorForwarder.test.ts 5 追加で 187→192）。fmt/lint/check 全段通過。**落とし穴**: ERR_FORWARDER は webview 内で実行される JS 文字列なので、実行時の挙動はループ内で直接テスト不可＝文字列に必須要素（D の name/message/stack 参照等）が含まれることの構造テストで代替した。`deno fmt` 必須。commit はこの ITERATE で。**残（LOOP_DONE まで）**: ②再 smoke で `[web] reject:` の**実メッセージ**を取得し、どの invoke コマンドが/なぜ reject するか特定→修正（白画面の真因）。③`pnpm test`(root)。**次周は再 smoke でメッセージ取得→原因コマンド特定が最優先タスク**。

- [m7:final-gate-deps] **[m7:final-gate] ①＝`package.json` から `@tauri-apps/*` 依存を全除去**（VISION M7 の「`package.json` からも `@tauri-apps/*` 依存を除去」）。フロントのコード import は [m7:dialog-open] までに全消去済み（`check-no-tauri` 0件）だったので、package.json の宣言だけが残っていた。削除したもの: `dependencies` から `@tauri-apps/api`(^2.10.1)・`@tauri-apps/plugin-dialog`(^2.6.0)・`@tauri-apps/plugin-store`(^2)、`devDependencies` から `@tauri-apps/cli`(^2.10.0)、加えて cli を呼ぶ `scripts.tauri`("tauri") も同系統整理として除去。`pnpm install` で 4 依存（@tauri-apps/api 2.10.1・plugin-dialog 2.6.0・plugin-store 2.4.2・cli 2.10.0）が node_modules から落ち、pnpm-lock.yaml も更新。**事前確認**: `grep -rn "@tauri-apps"` で src/ と deno-app/ に残る参照は**すべてコメント（説明文）のみ**で実 import は 0（`check-no-tauri` OK 再確認済み）＝削除しても import 解決は壊れない。**検証**: フロント別系統 `npx tsc --noEmit` クリーン・`pnpm lint`(biome 44 files) クリーン・`npx vitest run src/` 62 passed・`pnpm build`(tsc && vite) 成功（@tauri-apps 不在でも production bundle が解決＝webview shim 経由に完全移行済みを実証）。コミットゲート `cd deno-app && deno task verify` 187 passed（deno-app 不変）。**落とし穴**: なし（依存削除のみ、コードは無変更）。commit はこの ITERATE で。**残（LOOP_DONE まで＝[m7:final-gate] の②③）**: ②`cd deno-app && deno task smoke`（要 canary・PATH 先頭 `~/.deno-canary/bin`。pnpm build→desktop build→ヘッドレス起動→`[web]` 行に webview 実行時エラーが出ない＝白画面解消の最終ゲート）、③リポジトリルートで `pnpm test`（ただし `pnpm test`=`vitest run` はルートに vitest スコープ設定が無く deno-app の Deno テストを拾って失敗し得る＝[m3:rewire-invoke] 落とし穴。DoD が指す `pnpm test` の実体は要確認＝おそらく `npx vitest run src/` で既に緑、または smoke 側でカバー）。この2点が緑なら全 DoD 達成＝`LOOP_DONE`。**次周は smoke 実行を1タスクとして推奨**。

- [m7:dialog-open] **マイルストーン7⑥（最後の Tauri API 系統）＝`@tauri-apps/plugin-dialog` の `open({directory:true})`（`src/App.tsx` `handleAddFolder`＝お気に入りフォルダ追加 picker）を `open_folder` バインディング経由へ移行し、`check-no-tauri` を 1→0 件にした**。これまでブロック扱いだった（[m7:dialog-open-blocked]）が、代替案①＝OS ネイティブ picker を `Deno.Command` で起動する方式で実装。**ルーティングの設計判断**: window/event/menu は handleInvoke の専用 `*Fn` 引数で振り分けたが、picker は Desktop API ではなく標準 Deno（`Deno.Command`）＝`backend/` の純粋ロジック（trashFile の deleter 注入と同じ前例）に収まるため、**既存の invoke パイプライン（`bindings/invoke.ts` に `case "open_folder"`）にそのまま乗せた**。これで `main.ts`・`handleInvoke` シグネチャを一切変更せず最小差分で済む（`open_folder` は store_/window_/event_/menu_ のどの接頭辞でもないので handleInvoke の既定 invokeFn＝bindings/invoke.ts dispatch へ流れる）。実装3層: ①`deno-app/backend/dialog.ts`＝純粋ロジック。`buildFolderPickerCommand(os)`（darwin=`osascript -e 'POSIX path of (choose folder)'`／linux=`zenity --file-selection --directory`／windows=PowerShell `FolderBrowserDialog`、未対応 OS は throw）、`parsePickerOutput(stdout)`（trim＋ルート以外の末尾スラッシュ除去＝macOS の POSIX パスが `…/` で返るため。空はキャンセル相当で null）、`pickFolder(os?, runner?)`（既定 runner は `Deno.Command(...).output()` を実行、code≠0 または出力空＝キャンセルで null＝Tauri `open` のキャンセル時 null と等価）。runner 注入でテスト可能（GUI 不要）。②`bindings/invoke.ts` に `case "open_folder": return await pickFolder()`（引数なし）。③`deno-app/frontend/dialog.ts` に `open(options?, invokeFn?)` shim 追加＝`invoke<string|null>("open_folder")` を呼ぶ browser-safe コード（`ask` と同居、`frontend/invoke.ts` の `invoke` を import、テスト用 invokeFn 注入）。`src/` 差し替え: `src/api/dialog.ts`（既存）から `open`/`OpenOptions`/`OpenInvokeFn` を追加 re-export、`src/App.tsx` の `import { open } from "@tauri-apps/plugin-dialog"`→`./api/dialog`。`handleAddFolder` のロジック（open→addFavorite→state 更新）は不変（戻り値 `string|null` 互換）。**検証**: `deno task verify` 187 passed（backend/dialog.test.ts 11 + frontend/dialog.test.ts に open 2 追加）／`deno task check-no-tauri` **OK（0件）**／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功。**落とし穴**: `bindings/invoke.ts` の `open_folder` dispatch は引数検証パスが無く、実行すると既定 runner が実 GUI picker を起動するため、dispatch のユニットテストは書かず backend/dialog.ts 側で runner 注入により網羅した（trash_file の dispatch テストは検証エラーの純粋パスのみ＝同方針）。`deno fmt` 必須。commit はこの ITERATE で。**残（LOOP_DONE まで）**: ①`package.json` の `@tauri-apps/*` 依存（`@tauri-apps/api`・`plugin-dialog`・`plugin-store`・`cli`）を除去（VISION M7 の最終項目。コードからの import は全消去済みなので削除して `pnpm install`→build/tsc/lint/vitest 再確認）、②`deno task smoke`（pnpm build→desktop build→ヘッドレス起動→webview 実行時エラーゼロ＝白画面解消の最終ゲート、要 canary）、③`pnpm test`(root) のグリーン確認。これらが全部緑になれば全 DoD 達成＝LOOP_DONE。次周は **package.json 依存除去＋smoke 実行**を1タスクとして実施推奨。

- [m7:menu] **マイルストーン7⑤＝`@tauri-apps/api/menu`（ビューワーのコンテキストメニュー）を Deno Desktop の `showContextMenu` IPC 経由へ移行**。Tauri 版 `src/ViewerApp.tsx` は右クリックで `Menu`/`MenuItem`/`PredefinedMenuItem` からネイティブメニュー（見開き/単ページ切替・読み方向切替・Separator・Move to Trash・Separator・Close Window）を組み立て `menu.popup()` で表示し、各 `MenuItem.action` がクリックで webview 側実行されていた。**canary 調査（型プローブ）**: `Deno.BrowserWindow` に `showContextMenu(x:number, y:number, items: MenuItem[]):void` が存在（`setMenu`/`setApplicationMenu` も）。MenuItem の union は `{item:{label:string; id?:string; accelerator?:string; enabled:boolean}} | {submenu:{label,items}} | {role:{role:string}}`。クリックはメインプロセスに `contextMenuClick` イベント（`addEventListener("contextMenuClick", ...)` ＝`BrowserWindowEventMap`、ev は `CustomEvent`、detail は any）として返る。action コールバックは転送不能なので、**id だけ送りクリック時に webview の hook で action を引く**方式。実装3層: ①`deno-app/desktop/menu.ts`＝純粋ロジック。`toNativeMenuItems(items)`（`{type:"item",id,label}`→`{item:{label,id,enabled:true}}`、`{type:"separator"}`→`{role:{role:"separator"}}`）、`handleMenuCommand(win, "menu_popup", {x,y,items})`（座標/配列検証→`win.showContextMenu`）、`extractMenuClickId(detail)`（**string と `{id}` の両形に対応＝detail 形が canary で不確定なためヘッジ**、取れねば null）、`menuClickScript(id)`（`globalThis.__mekuriMenuClick&&...(id)` の存在チェック付き JS）。②`deno-app/frontend/menu.ts`＝browser-safe な Tauri 互換 shim。`MenuItem.new({text,action})`（連番 id 採番）/`PredefinedMenuItem.new({item})`（Separator のみ→separator ノード）/`Menu.new({items})`→`MenuHandle`。`popup(at?, host?, invokeFn?)` は host の `__mekuriMenuActions` registry を**毎回 clear して入れ替え**（前回 popup の action を残さない）、`__mekuriMenuClick(id)` hook を初回設置、項目をシリアライズして `invoke("menu_popup",{x,y,items})`。座標は ViewerApp が渡す（下記）。③配線: `desktop/bridge.ts` の `handleInvoke` に第6引数 `menuFn?`（`menu_*` 接頭辞、window/store/event と同パターン）追加。`main.ts` の `bindInvoke(target)` に `(c,a)=>handleMenuCommand(target,c,a)` を渡し、**かつ各窓に `target.addEventListener("contextMenuClick", ev=>{ id=extractMenuClickId((ev as CustomEvent).detail); if(id) target.executeJs(menuClickScript(id)) })` を設置**（クリック→webview の action 実行）。`src/` 差し替え: `src/api/menu.ts`（`frontend/menu.ts` の Menu/MenuItem/PredefinedMenuItem re-export、`src/api/event.ts` と同方式）を新設し、ViewerApp の**動的 `await import("@tauri-apps/api/menu")`→静的 `import { Menu, MenuItem, PredefinedMenuItem } from "./api/menu"`**。`menu.popup()`→`menu.popup({x:e.clientX, y:e.clientY})`（contextmenu の MouseEvent 座標を渡す＝Tauri は popup() が自動でカーソル位置に出すが showContextMenu は座標必須なため。ViewerApp の差し替えは VISION で許可された配線範囲）。**設計判断**: クリック配送は [m7:event] と同じ `executeJs` プッシュ機構を流用（canary に main→webview の専用イベント機構が無い）。registry を host 注入可能にしテスト容易化（既存 shim 群と同パターン）。**未確定リスク（要 smoke 実機確認）**: (a) `contextMenuClick` の detail 実体（string か `{id}` か）＝両対応でヘッジ済、(b) `role:"separator"` が実機で区切り線になるか。型は通るが GUI 実行は本ループで自動検証不可（smoke が最終ゲート）。**検証**: `deno task verify` 174 passed（desktop/menu.test.ts 9 + frontend/menu.test.ts 7 + bridge.test.ts に menu ルーティング 2 追加＝計18）／`deno check main.ts`(canary 型)クリーン／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功。`check-no-tauri` は **2→1 件**へ減少（menu 系統消去）。**落とし穴**: `assertRejects` を返すテストは `Deno.test(name, async()=>{ await assertRejects(...) })` にしないと `Promise<Error>` 型で fail（`return assertRejects` は不可）。`deno fmt` 必須。commit はこの ITERATE で。**残（M7 最後の1系統）**: `check-no-tauri` 残1件＝`src/App.tsx`(plugin-dialog **open**＝フォルダ picker＝[m7:dialog-open-blocked]）。ネイティブ picker op が canary に無いため、`Deno.Command` で OS picker（macOS=`osascript -e 'choose folder'` 等）を起動する `open_folder` バインディングを `main.ts`/`desktop/` に足す方式で実装が必要。これが終われば `check-no-tauri` 0件→ `package.json` の `@tauri-apps/*` 依存削除 → `smoke` 確認で M7/全 DoD 完了。

- [m7:event] **マイルストーン7④＝`@tauri-apps/api/event`（窓間 pub/sub の `listen`/`emit`）を Deno Desktop IPC 経由へ移行**。Tauri 版はビューワー窓 `src/ViewerApp.tsx` の `emit("file-trashed")`（ゴミ箱移動後）をメイン窓 `src/components/FileList/FileList.tsx` の `listen("file-trashed", ...)` が受けてファイル一覧を再読込していた。Deno Desktop には窓間イベント機構が無いため、**webview→メインの IPC（`invoke("event_emit")`）でメインへ送り、メインから全窓へ `executeJs` でブロードキャスト**する形に置換。実装3層: ①`deno-app/desktop/event.ts`＝`handleEventCommand(windows, command, args)` 純粋ディスパッチャ。`windows()` は**配信時点で開いている全窓を返すサンク**（後から開いたビューワー窓も受信できるよう live set）。`deliverScript(event,payload)` で各 webview の delivery hook（`globalThis.__mekuriDeliverEvent`）を呼ぶ JS スニペット（event/payload は JSON 埋め込み、フック未設置の窓でも存在チェックで no-op）を組み立て `BroadcastTarget.executeJs` で実行。`event_emit` 以外と非 string event は reject。②`deno-app/frontend/event.ts`＝browser-safe な Tauri 互換 shim。`listen(event,handler,host?)` は host（既定 `globalThis`）の registry（`__mekuriEventHandlers: Map<string,Set>`）へ登録し、初回に delivery hook を設置（配信中の unlisten に備え `[...set]` スナップショットで回す）、`Promise<UnlistenFn>` を返す。`emit(event,payload?,invokeFn?)` は `invoke("event_emit",{event,payload:payload??null})`（payload 省略時 null）。③配線: `desktop/bridge.ts` の `handleInvoke` に第5引数 `eventFn?`（`event_*` 接頭辞を振り分け、store/window と同パターン）を追加。`main.ts` に `liveWindows()`（メイン窓＋未クローズ viewerWindows）を定義し `bindInvoke` で `(c,a)=>handleEventCommand(liveWindows,c,a)` を渡す（viewerWindows Map を bindInvoke 定義前へ移動）。`src/` 差し替え: 境界 import を `src/api/event.ts`（`deno-app/frontend/event.ts` の `listen`/`emit` re-export、`src/api/window.ts` と同方式）に局所化し、FileList の `import { listen } from "@tauri-apps/api/event"`→`../../api/event`、ViewerApp の動的 `await import("@tauri-apps/api/event")`→静的 `import { emit } from "./api/event"`。**設計判断**: main→webview のプッシュは canary に専用イベント機構が無いため `executeJs(code)`（[m4:main] で利用可能と確認済みのメソッド）で各 webview の delivery hook を呼ぶ方式を採用。registry を host 注入可能にしテスト容易化（window/dialog/invoke と同じ注入パターン）。`UnlistenFn` 型は window.ts も同名 export するため event からは re-export せず衝突回避。**検証**: `deno task verify` 157 passed（desktop/event.test.ts 6 + frontend/event.test.ts 7 + bridge.test.ts に event ルーティング 2 追加＝計15）／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功。`check-no-tauri` は **4→2 件**へ減少（event 系統 listen/emit 消去）。**落とし穴**: `deno fmt` で test の複数行呼び出しが整形対象になるので編集後 `deno fmt` 必須。commit はこの ITERATE で。**残（M7）**: `check-no-tauri` 残2件＝`src/App.tsx`(plugin-dialog **open**＝フォルダ picker＝**ブロック**、[m7:dialog-open-blocked] 参照)、`src/ViewerApp.tsx`(api/menu＝コンテキストメニュー)。次系統は **menu**（`@tauri-apps/api/menu` の Menu/MenuItem/PredefinedMenuItem＝ビューワーのコンテキストメニュー。canary に `showContextMenu`/`setMenu` op あり＝[m7:dialog-ask] の canary 調査参照）→ 最後に **open(picker)**（ネイティブ picker をメイン側バインディングで用意）の順。

- [m7:dialog-ask] **マイルストーン7③（dialog 系統の前半）＝`@tauri-apps/plugin-dialog` の `ask`（確認ダイアログ）を webview 標準 `confirm()` 経由へ移行**。Tauri 版 `src/components/FileList/FileList.tsx`（ゴミ箱移動の確認）と `src/ViewerApp.tsx`（同）は `ask(message, {title, kind:"warning"})` で boolean を得ていた。**重要な発見（canary 調査）**: Deno Desktop canary 2.9.0 の利用可能な desktop op は `op_desktop_{alert,confirm,prompt,init,recv_event,resolve/reject_bind_call,send_error_report,notification_*,verify_ed25519,apply_patch}` のみで、**ネイティブのファイル/フォルダ選択 picker op は存在しない**（binary の strings 調査で確認。`Deno.BrowserWindow` のメソッドにも `*Dialog`/`pickFolder` 系は無く、あるのは `setApplicationMenu`/`showContextMenu`/`setMenu`/`getNativeWindow`/`executeJs`/`setResizable`/`setAlwaysOnTop` 等）。よって dialog 系統は2分割: **`ask`（確認・boolean）は実装可能／`open`（フォルダ picker）はブロック**。`ask` は webview の標準 `confirm()`（WKWebView/WebView2/WebKitGTK が標準サポート）で等価実装＝確認ゲートという機能は保持され、失われるのは title/kind のダイアログ装飾のみ（VISION の「機能を削らない」を満たす）。実装: ①`deno-app/frontend/dialog.ts`＝browser-safe な `ask(message, options?, confirmFn=defaultConfirm)`（既定は `globalThis.confirm`、未存在なら throw。テスト用に confirmFn 注入可＝`frontend/invoke.ts`/`window.ts` と同じ注入パターン）＋`frontend/dialog.test.ts`(4)＝true/false 解決・message 転送＆options 無視・既定 globalThis.confirm 経路。②`frontend/mod.ts` から re-export。③`src/api/dialog.ts`＝境界 import 局所化（`../../deno-app/frontend/dialog.ts` 相対 `.ts`、`src/api/window.ts` と同方式）。④`src/components/FileList/FileList.tsx`・`src/ViewerApp.tsx` の `import { ask } from "@tauri-apps/plugin-dialog"` を `../../api/dialog`・`./api/dialog` へ。**設計判断**: confirm はメイン側 `op_desktop_confirm`（Deno global `confirm`）への IPC ブリッジ案もあったが、(a) Deno の `confirm` は同期ブロッキングで bind ハンドラ内で main スレッドを塞ぐ恐れ、(b) webview に標準 `confirm` があり IPC 往復不要、なので webview 側 `confirm` を採用（window_*/store_* の IPC ブリッジは不要なケース）。**検証**: `deno task verify` 142 passed（dialog.test.ts 4 追加）／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功。`check-no-tauri` は **6→4 件**へ減少（`ask` 2件消去）。**落とし穴**: `deno fmt --check` で test の複数行 `ask(...)` 呼び出しが整形対象になるので編集後 `deno fmt` 必須。commit はこの ITERATE で。**残（M7）**: `check-no-tauri` 残4件＝`src/App.tsx`(plugin-dialog **open**＝フォルダ picker＝**ブロック**)、`src/components/FileList/FileList.tsx`(api/event listen)、`src/ViewerApp.tsx`(api/event emit / api/menu)。次系統は **event**（`@tauri-apps/api/event` の listen/emit＝窓間 pub/sub）→ menu の順。`open`（フォルダ picker）はネイティブ picker op が無く現状ブロック＝別途 osascript 等のネイティブ picker をメイン側バインディングで用意するか、Deno Desktop の picker 追加待ち（Open 参照）。

- [m7:webviewwindow] **マイルストーン7②＝`@tauri-apps/api/webviewWindow`（ビューワー窓生成）を既存 `open_viewer` バインディングへ移行**。Tauri 版 `src/App.tsx` の `handleArchiveSelect` は `WebviewWindow.getByLabel(label)`→既存なら `setFocus`／無ければ `getViewerSettings()` で `new WebviewWindow(label, {url,title,width,height,minWidth,minHeight,visible})` を生成し `once("tauri://error",...)` していた。この一連（label 算出・二重オープン防止・保存サイズ生成・focus）は**既に `main.ts` の `open_viewer` バインディングが内包**していた（[m4b:viewer] の `desktop/viewer.ts` 純粋ロジックを注入）。よって移行は webview 側 shim 1 本＋App.tsx の差し替えで済む。実装: ①`deno-app/frontend/viewer.ts`＝browser-safe な `openViewer(archivePath, resolver?)`（`globalThis.bindings.open_viewer(archivePath)` を呼ぶ。`frontend/invoke.ts` と同じ resolver 注入でテスト容易化、既定リゾルバは bindings/open_viewer 不在を検知して投げる）＋`frontend/viewer.test.ts`(3)＝転送/既定リゾルバ不在 reject/binding エラー伝播。②`frontend/mod.ts` から re-export。③`src/api/viewer.ts`＝境界 import 局所化（`../../deno-app/frontend/viewer.ts` 相対 `.ts`、[m7:window] の `src/api/window.ts` と同方式）。④`src/App.tsx`: `@tauri-apps/api/webviewWindow` import 撤去、`handleArchiveSelect` を `await openViewer(archivePath)`（try/catch でエラーログ＝旧 `once("tauri://error")` 相当）へ、不要 import（`WebviewWindow`/`getViewerSettings`/`viewerLabel`/`VIEWER_MIN_WIDTH`/`VIEWER_MIN_HEIGHT`）を整理（`fileNameFromPath` は title 用に残置）。**設計判断**: `open_viewer` は generic `invoke` バインディングとは別の `win.bind("open_viewer", ...)`（positional 引数）。よって shim は `invoke` を経由せず `bindings.open_viewer(archivePath)` を直接呼ぶ専用 resolver を持つ。**等価化**: ついでに `main.ts` の `loadViewerSettings` を `() => DEFAULT_VIEWER_SETTINGS`（M5 まで既定値の暫定）から `() => getViewerSettings(store)` へ配線し、Tauri 版のビューワーサイズ復元挙動と等価にした（store は既に main.ts で open 済み・getViewerSettings は backend/mod.ts から export 済み）。これで [next2]/viewer-size の暫定ギャップも解消。**未対応（canary 制約・既知）**: `minWidth`/`minHeight`/`visible` は canary `BrowserWindowOptions` に無く未表現（[m4b:viewer] と同じ）。**検証**: `deno task verify` 138 passed（viewer.test.ts 3 追加）／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功。`check-no-tauri` は **7→6 件**へ減少（webviewWindow 系統消去）。commit はこの ITERATE で。**残（M7 次系統＝dialog→event→menu の順）**: `check-no-tauri` 残6件＝`src/App.tsx`(plugin-dialog open)、`src/components/FileList/FileList.tsx`(api/event listen / plugin-dialog ask)、`src/ViewerApp.tsx`(plugin-dialog ask / api/event emit / api/menu)。次は **dialog**（`@tauri-apps/plugin-dialog` の `open`/`ask`）系統＝ネイティブダイアログのバインディングを `main.ts` に足し browser-safe shim 経由へ。

- [m7:window] **マイルストーン7①＝`@tauri-apps/api/window` 系統を Deno Desktop IPC へ移行**。白画面の根本原因（webview に `__TAURI_INTERNALS__` 無し）対策の第1系統。Tauri 版の `getCurrentWindow()`（`setSize`/`getSize`/`setTitle`/`show`/`close`/`onResized`）と `LogicalSize` を、`invoke("window_*")` 経由のブリッジへ置換。実装3層: ①`deno-app/desktop/window.ts`＝`handleWindowCommand(win, command, args)` 純粋ディスパッチャ（`ControllableWindow` インターフェース＝`Deno.BrowserWindow` のサブセットを構造的に満たす窓を注入。`window_set_size`/`window_get_size`(`{width,height}` を返す)/`window_set_title`/`window_show`/`window_close`、引数検証・未知コマンドは reject）。②`deno-app/frontend/window.ts`＝browser-safe な Tauri 互換 shim（`LogicalSize` クラス・`getCurrentWindow()`→`AppWindow`。各メソッドは `frontend/invoke.ts` の `invoke` を呼ぶ。`onResized` は **DOM `resize` イベント**を契機に `window_get_size` で実 OS サイズを取得し Tauri 互換 `{payload}` でハンドラへ＝サイズ保存/復元の挙動を等価に保つ。invoke/resizeTarget を注入可能にしテスト容易化）。③配線: `desktop/bridge.ts` の `handleInvoke` に第4引数 `windowFn?`（`window_*` 接頭辞を振り分け、storeFn と同パターン）を追加し、`main.ts` の `bindInvoke(target)` で各窓自身を束縛した `handleWindowCommand(target, ...)` を渡す（メイン窓・各ビューワー窓とも IPC 取得）。`src/` 差し替え: 境界 import を `src/api/window.ts`（`deno-app/frontend/window.ts` の re-export、`src/api/invoke.ts` と同方式）に局所化し、`src/App.tsx`/`src/ViewerApp.tsx`/`src/hooks/useWindowResize.ts`/`src/hooks/useSiblingNavigation.ts` の `@tauri-apps/api/window` import を `./api/window`・`../api/window` へ。**設計判断**: `getCurrentWindow()` の窓特定は `globalThis.bindings` が webview ごとに別＝各窓の bind 済み `window_*` ハンドラが自窓を操作するので、shim 側は窓を区別せず invoke するだけでよい（main 側で `bindInvoke(target)` が target を閉包）。resize 検知に Tauri の `onResized` 相当 API が canary `Deno.BrowserWindow` に無いため DOM resize で代替（payload は `window_get_size` で実サイズ取得＝コンテンツサイズずれを回避）。**検証**: `deno task verify` 135 passed（window.test.ts 5 + window 用 bridge.test.ts 2 + desktop/window.test.ts 7 追加）／フロント別系統 `npx tsc --noEmit` クリーン・`npx vitest run src/` 62 passed・`pnpm lint`(biome) クリーン・`pnpm build` 成功（クロス境界 import が production bundle へ解決）。`check-no-tauri` は 11→**7 件**へ減少（window 系統4件消去）。**落とし穴**: `ControllableWindow` に `close()` を後から追加（ViewerApp が `getCurrentWindow().close()` を使うため。desktop test の fakeWindow にも close 追加要）。`@std/assert` 使用テストの import 整形は `deno fmt` で1行化されるので編集後 `deno fmt` 必須。commit はこの ITERATE で。**残（M7 次系統）**: `check-no-tauri` 残7件＝`src/App.tsx`(WebviewWindow / plugin-dialog open)、`src/components/FileList/FileList.tsx`(api/event listen / plugin-dialog ask)、`src/ViewerApp.tsx`(plugin-dialog ask / api/event emit / api/menu)。次は webviewWindow→dialog→event→menu の順で1周1系統。

- [m6:build] **マイルストーン6 完了 ＝ 全マイルストーン達成（LOOP_DONE）**。canary deno 2.9.0（`deno desktop` 入り、PATH 先頭・`deno desktop --help` 確認済み）で `deno desktop deno-app/main.ts` のビルド成功を確認した。手順: ①リポジトリルートで `pnpm build`（`dist/` 生成、Vite ビルド成功）→ ②`cd deno-app && deno desktop --output /tmp/mekuri-m6-build/Mekuri.app main.ts`（GUI を開いてブロックしないよう `--output` でアプリバンドル生成のみを狙った）。結果: **exit 0**、`Check main.ts`→`Compile main.ts to Mekuri.dylib`→Embedded Files（backend/bindings/desktop/main.ts ＋ node-unrar-js/trash 等の npm 依存を埋め込み）→`Codesigning bundle with identity "-"`→`Bundle .../Mekuri.app` まで完走。生成物は `Mekuri.app/Contents/MacOS/{Mekuri,Mekuri.dylib,laufey_webview}` ＋ `Info.plist` ＋ `_CodeSignature/`（laufey-webview backend v0.4.0 を自動 DL して同梱・コード署名）。**検証**: ビルド exit 0 ＝ `deno check main.ts`（canary の `Deno.BrowserWindow` 型）含めビルドパイプライン全段通過＝ M6 DoD「ビルドでき」を担保。`deno task verify` も 120 passed 維持（main.ts/desktop は不変）。**目視起動確認は operator note の方針どおり人間に委ねる**（GUI 表示は自動検証困難）。**落とし穴**: macOS に `timeout` コマンドが無い（`gtimeout` も未導入）ので、ブロッキングし得るデスクトップ起動は避け `--output` でバンドル生成に徹した（`deno desktop main.ts` を引数無し output で実行すると window を開いて待機する恐れ）。ビルドの副作用はリポジトリに残らない（output は `/tmp`、`dist/` は .gitignore 済み＝`git status` クリーン、コミット対象は MEMORY.md のみ）。commit はこの ITERATE で。これで VISION.md の「完了の定義」全項目（M1〜M6）がグリーン。

- [m3:store-swap] **マイルストーン3 完了**。store系配線の最終段③＝`src/api/store.ts` の `import { load } from "@tauri-apps/plugin-store"` を webview shim へ差し替えた（`import { load } from "../../deno-app/frontend/store.ts"`）。[m3:rewire-invoke] と同じく `.ts` 拡張子付き相対 import で tsc(`moduleResolution: bundler`+`allowImportingTsExtensions`)・Vite とも解決。差分は import 元 1 行のみ（`getStore` のロジック不変、`favorites.ts`/`settings.ts` は無改変で動く＝両者は `getStore().get/set` を使うだけ）。これで M3 の DoD（`src/api/` 全 5 本を Deno バインディング呼び出しへ切替・`src/components/` 無改変）を達成: directory.ts/archive.ts は invoke shim 経由（[m3:rewire-invoke]）、favorites.ts/settings.ts/store.ts は store shim 経由（getStore→WebviewStore→`invoke("store_get"/"store_set")`→main.ts の `handleStoreCommand`）。**検証**: `src/` は `deno task verify` 対象外＝別系統で実施し、`deno task verify` 120 passed（deno-app 不変）/ `npx tsc --noEmit` クリーン / `npx vitest run src/` 62 passed / `pnpm lint`(biome) クリーン / `pnpm build` 成功（クロス境界 import が production bundle に解決）。落とし穴: フロント検証で `pnpm test` は使わない（ルートに vitest 設定が無く deno-app の Deno テストを拾って失敗＝[m3:rewire-invoke] 参照）。commit はこの ITERATE で。**残るマイルストーンは M6 のみ**（`deno desktop deno-app/main.ts` のビルド成功確認、canary 導入済み）。

- [m3:store-wire] store系 IPC 配線の「残」②＝`main.ts` 合流配線を完了。`desktop/bridge.ts` の `handleInvoke` に第3引数 `storeFn?: StoreFn`（`(command, args)=>Promise<unknown>`）を追加し、`store_*`（接頭辞 `store_`）コマンドは `storeFn` へ、それ以外は従来どおり `invokeFn` へ振り分けるようにした。`storeFn` 省略時は store コマンドも `invokeFn` へ流れる（後方互換、既存6テスト不変）。`main.ts` では `const store = await Store.load(settingsPath(), {autoSave:true})` で [m5:paths] の `settingsPath()`（Tauri と同一 settings.json パス）を開き、`bindInvoke` の `win.bind("invoke", ...)` で `handleInvoke(args, undefined, (cmd,a)=>handleStoreCommand(store,cmd,a))` を呼ぶ形に変更（invokeFn は `undefined` で既定 `defaultInvoke` を適用）。メイン窓・各ビューワー窓とも同一 `store` インスタンスを共有（`bindInvoke` 経由）。`desktop/mod.ts` から `StoreFn` を re-export。`bridge.test.ts` に3件追加（store_* は storeFn へ／非 store は invokeFn へ／storeFn 無しでは store_* も invokeFn へフォールバック）。設計判断: ルーティングを純粋関数 `handleInvoke` に置きテスト可能に保ち、`main.ts` は Store 生成（副作用）と注入のみ＝`backend/`/`desktop/` の Desktop 非依存方針を維持。落とし穴: `main.ts` の import 整形は `deno fmt --check` で fail するので編集後 `deno fmt main.ts` を必ず実行（複数行 import が1行化された）。VERIFY グリーン（120 passed）。commit はこの ITERATE で。**残（store系配線の最終段＝③）**: `src/api/store.ts` の `getStore` 内 `import { load } from "@tauri-apps/plugin-store"` を webview shim（`deno-app/frontend/store.ts` の `load`）へ、[m3:rewire-invoke] と同じ `../../deno-app/frontend/store.ts` 相対 `.ts` import で差し替える（`favorites.ts`/`settings.ts` は無改変で動く）。これは `src/` 側のため `deno task verify` の対象外＝フロント検証は `npx tsc --noEmit`/`npx vitest run src/`/`pnpm lint`/`pnpm build`（`pnpm test` は使わない＝[m3:rewire-invoke] 落とし穴参照）。

- [m3:store-shim] [m3:store-dispatch] の「残」3段の①＝webview 側 plugin-store 互換 Store shim を `deno-app/frontend/store.ts`（+ `frontend/store.test.ts` 4件）として新設。Tauri 版 `src/api/store.ts` の `getStore` は `@tauri-apps/plugin-store` の `load(name,{defaults,autoSave})` → `store.get(key)`/`store.set(key,value)` を使う（`favorites.ts`/`settings.ts` が依存）。この shim は `WebviewStore` クラス（`get<T>(key)`→`invoke("store_get",{key})`、`set(key,value)`→`invoke("store_set",{key,value})`）＋ plugin-store 互換の `load(name,options)`（`new WebviewStore()` を返すだけ）を提供。`frontend/invoke.ts` の `invoke` を使う **browser-safe**（`backend/` 非 import）コードで、`frontend/invoke.ts` と同じ注入パターン＝`WebviewStore` のコンストラクタに `StoreInvoke` を注入可能にしテスト容易化（既定は実 `invoke`）。設計判断: `defaults`/`autoSave` はアプリ層（`main.ts`）が実 `Store.load(settingsPath(), {defaults,autoSave})` を開く際に適用するため、shim では受け取るだけで未使用（plugin-store と同じ呼び出し形を保ち `src/api/store.ts` を最小差し替えで済ます）。`store_get` の未設定キー `null` は `handleStoreCommand` 仕様どおりで frontend が `?? default` で吸収。`frontend/mod.ts` から `load`/`WebviewStore`/型を re-export。`frontend/` は既に deno.json の verify/check(mod.ts)/test 対象なので deno.json 変更不要。VERIFY グリーン（117 passed）。commit はこの ITERATE で。**残（store系配線の次段）**: ②`main.ts` で `Store.load(settingsPath(), {defaults, autoSave:true})`（[m5:paths] の `settingsPath()` 利用）し、`win.bind("invoke", ...)` のディスパッチで command が `store_*` なら `handleStoreCommand(store, command, args)` へ、それ以外は既存 `handleInvoke` へ合流させる。③`src/api/store.ts` の `getStore` の `import { load } from "@tauri-apps/plugin-store"` を、この shim（`../../deno-app/frontend/store.ts` 相対 `.ts` import、[m3:rewire-invoke] と同じ解決方式）へ差し替え（`favorites.ts`/`settings.ts` は無改変で動く）。フロント検証は `npx tsc --noEmit`/`npx vitest run src/`/`pnpm lint`/`pnpm build`（`pnpm test` は使わない＝[m3:rewire-invoke] の落とし穴参照）。

- [m3:store-dispatch] M3/M5 の store系 IPC 配線の核 = `deno-app/bindings/store.ts` を新設。`invoke.ts`（ステートレスに `backend/` の関数を呼ぶ）に対し、こちらはアプリ層が開いた `backend/store.ts` の `Store` インスタンスへ store コマンドを振り分ける**ステートフル**ディスパッチャ `handleStoreCommand(store, command, args)`。対応コマンドは frontend（`src/api/store.ts`→`favorites.ts`/`settings.ts`）が実際に使う `store.get(key)`/`store.set(key,value)` のみに合わせて `store_get`/`store_set` の2つ（最小変更）。`store_get` は未設定キーで `null` を返す（Tauri plugin-store と同じく JSON セーフに、frontend は `?? default` で吸収）。`store_set` は autoSave Store ならファイル永続化される。引数名は frontend が渡す形を想定し `key`/`value`。`bindings/mod.ts` から `handleStoreCommand` を re-export。`store.test.ts`(6)＝null/既定値取得・set→get・autoSave永続化・key欠落reject・未知コマンドreject。`bindings/` は既に deno.json の verify/check(mod.ts)/test 対象なので deno.json 変更不要。VERIFY グリーン（113 passed）。commit はこの ITERATE で。**残（store系配線の次段）**: ①webview shim＝`deno-app/frontend/store.ts` に plugin-store の `load()` 互換 Store ラッパ（`bindings.invoke("store_get"/"store_set", {...})` を呼ぶ、`frontend/invoke.ts` と同パターンの browser-safe コード）を置く、②`main.ts` で `Store.load(settingsPath(), {defaults, autoSave:true})`（[m5:paths] の `settingsPath()` 利用）して `win.bind("invoke", ...)` のディスパッチに `handleStoreCommand` を合流させる（command が `store_*` なら store へ、それ以外は既存 `handleInvoke` へ）、③`src/api/store.ts` の `getStore`（`@tauri-apps/plugin-store` の `load`）を webview shim へ差し替え（`src/api/invoke.ts` と同様 `../../deno-app/frontend/...` 相対 `.ts` import で、`favorites.ts`/`settings.ts` は無改変で動く）。

- [m3:rewire-invoke] マイルストーン3の実差し替え第一弾。`src/api/directory.ts` と `src/api/archive.ts` の `import { invoke } from "@tauri-apps/api/core"` を、新設した `src/api/invoke.ts` 経由の Deno Desktop shim へ差し替えた。境界をまたぐ import（`src/` → `deno-app/`）を 1 ファイルに局所化するため、`src/api/invoke.ts` は `deno-app/frontend/invoke.ts`（[m3:shim] の browser-safe Tauri 互換 invoke）の `invoke` と型（`InvokeArgs`/`DesktopBindings`/`BindingsResolver`）を re-export するだけにし、他の api は `./invoke` を見る設計にした。コマンド名・引数は不変なのでロジック変更なし（差分は import 元のみ）。**import パス解決の決定**（[m3:shim] 末尾の宿題）: tsconfig は `moduleResolution: bundler` + `allowImportingTsExtensions: true` のため、`../../deno-app/frontend/invoke.ts`（`.ts` 拡張子付き相対 import）で tsc・Vite とも解決できた（Vite alias は不要）。**検証**（`src/` は `deno task verify` 対象外＝別系統で実施）: `npx tsc --noEmit` クリーン / `npx vitest run src/` 62 passed / `pnpm lint`(biome) クリーン / `pnpm build`(tsc && vite build) 成功＝クロス境界 import が production bundle に解決されることを確認（dist 生成、dist は .gitignore 済み）。`deno task verify` も 107 passed 維持（deno-app は不変）。`src/components/` は無改変（RULES 遵守）。**落とし穴**: ルートに vitest 設定が無く `pnpm test`(=`vitest run`) は `deno-app/**/*.test.ts`（`@std/assert` を使う Deno テスト）も拾って 14 ファイル失敗する（既存の想定内ミスマッチ、本変更とは無関係）。フロント検証は必ず `npx vitest run src/` でスコープすること。commit はこの ITERATE で。**残**: M3 の `favorites.ts`/`settings.ts`/`store.ts` は invoke ではなく `@tauri-apps/plugin-store`（`load()`）に依存しており、Deno 側 Store（`backend/store.ts`/`settings.ts`、M5 実装済み）への配線にはアプリ層（`main.ts`）で Store を開いて bindings 経由で公開する仕組みが要る＝M5 アプリ層配線とセットで対応。次は M6（`deno desktop main.ts` ビルド）または store 系配線。

- [m3:shim] マイルストーン3 の最初の検証可能な一歩＝webview 側の browser-safe な Tauri 互換 `invoke` shim を `deno-app/frontend/invoke.ts`（+`frontend/mod.ts` 公開、`invoke.test.ts` 5件）として新設。Tauri 版フロント（`src/api/*.ts`）は `@tauri-apps/api/core` の `invoke(command, args)` を使っていたが、これを Deno Desktop の webview 側 API＝`globalThis.bindings.invoke(command, args)`（M4 `main.ts` の `win.bind("invoke", ...)` ／`desktop/bridge.ts` の `handleInvoke` が受ける IPC ブリッジの webview 側）を呼ぶ shim へ置き換える。設計: `backend/` を一切 import しない（=Vite ビルドを壊さない）ブラウザセーフコードに保つのが肝。公開シグネチャは Tauri と同一の `invoke<T>(command, args?)`。テスト容易性のため第3引数 `resolver: BindingsResolver`（既定は `globalThis.bindings` を返し未注入なら throw）を注入可能にし、`src/api/*.ts` は2引数のまま呼べる。型 `InvokeArgs`/`DesktopBindings`/`BindingsResolver` を定義。`deno.json` の verify/check/test/fmt/lint へ `frontend/` を追加（check は `frontend/mod.ts`）。VERIFY グリーン（107 passed）。commit はこの ITERATE で。**残（次タスク）**: 実際に `src/api/*.ts`（directory.ts/archive.ts から）の `import { invoke } from "@tauri-apps/api/core"` をこの shim へ差し替える。import パス解決は要 PLAN＝`src/` は Vite/tsc ビルドで deno verify 対象外なので、(a) shim を `src/` 配下へコピー/再エクスポートするか、(b) Vite alias / tsconfig paths で `deno-app/frontend/invoke.ts` を解決するかを決める。差し替え後は `pnpm test`(vitest)・`npx tsc --noEmit` でフロント側が壊れないことを手動確認（deno verify とは別系統）。`src/components/` は無改変（RULES）。

- [m4b:viewer] マイルストーン4② 完了。ビューワー窓生成 + **同一アーカイブ二重オープン防止**を `deno-app/desktop/viewer.ts`（+`viewer.test.ts` 10件）に純粋ロジックとして実装（`src/App.tsx` の `handleArchiveSelect` / `src/utils/windowLabel.ts` 相当）。`hashCode`/`viewerLabel`/`fileNameFromPath`/`viewerWindowTitle`(`<name> - mekuri`)/`viewerUrl`(archive パスを encode)/`viewerWindowOptions`(保存サイズ＋既定 `DEFAULT_VIEWER_SETTINGS`)/`openViewer`(既存 label の窓があれば focus し新規作成しない＝二重オープン防止、無ければ注入 creator で生成)。実窓生成・focus・登録は `main.ts` が注入（Desktop 非依存維持）。`desktop/mod.ts` から re-export、`main.ts` に viewer オープン配線を追加。VERIFY グリーン（102 passed）。注: canary に minWidth/visible 無しのためビューワー最小サイズ 600x400 は未対応。**このコミットは session limit 中断で agent が未コミットだった分を、オペレータが `deno fmt`+全 VERIFY で確定したもの。** これで M4（①main+bridge=5ee3c2e / ②viewer+dedup）完了。次は operator note 手順② = M3。

- [m4:main] マイルストーン4 の核 ①（operator note の手順①）に着手＝Deno Desktop エントリポイント `deno-app/main.ts` を新設し、メイン窓起動 + webview→Deno メインの **invoke IPC ブリッジ**を確立。Tauri 版 `WebviewWindow`+IPC を `Deno.BrowserWindow`+bindings へ置換。設計: Desktop API 非依存の純粋ロジックを `deno-app/desktop/` に分離（`backend/`/`commands` 分離方針を踏襲）＝(a) `bridge.ts` の `handleInvoke(rawArgs, invokeFn=bindings.invoke)`＝webview の `bindings.invoke(command,args)` で届く可変長引数 `[command, args?]` を検証し `bindings/mod.ts` の invoke へディスパッチ（command 非 string / args 非 object は throw、null args は undefined 化）、(b) `windowConfig.ts` の `mainWindowOptions(settings?)`＝保存サイズ or `DEFAULT_MAIN_SETTINGS`(1000x700)・title "mekuri"・resizable を導出。`desktop/mod.ts` から re-export。`main.ts` は配線のみ＝`Deno.serve`+`@std/http` の `serveDir` で `../dist/` 配信（webview 自動遷移、dist は M6 ビルドで生成）、`new Deno.BrowserWindow(mainWindowOptions())`、`win.bind("invoke", (...args)=>handleInvoke(args) as BridgeReturn)`、`win.show()`。
  - **canary 型の重要な発見**: `Deno.BrowserWindow` は通常の `deno check` の型 lib に**無い**。`main.ts` 冒頭に `/// <reference lib="deno.desktop" />`（lib 名は `lib.deno.desktop.d.ts`＝`asset:///` 埋め込み）を置くと `deno check main.ts` で型が通る。よって verify は operator note 通り `deno check ... main.ts` を追加できた（canary の `deno check` で OK、`deno desktop` 実行は不要）。
  - **canary API の実測（型検査で確認）**: `new Deno.BrowserWindow(opts)` の `BrowserWindowOptions` 有効キーは `title/width/height/x/y/resizable/alwaysOnTop/frameless/noActivate/transparentTitlebar` のみ。**`minWidth`/`minHeight`/`visible` は無い**（Tauri 版の最小サイズ 600x400 制約・visible:false は表現不可＝未対応のまま）。`setMinSize` も無い。利用可能メソッド: `show/hide/focus/close/reload/navigate(url)/executeJs(code)/setTitle/setSize/getSize/setPosition/getPosition/setResizable/setAlwaysOnTop/openDevtools/getNativeWindow/isClosed/isVisible/...` と `bind(name, handler)`。bindings モデル: メイン側 `win.bind(name, async (...args)=>...)` / webview 側 `await bindings.name(args)`、引数・戻り値は JSON（+Uint8Array）。bind ハンドラ戻り値型は内部型 `BrowserWindowReturn`（非公開）なので `unknown` 直返しは不可＝`Awaited<ReturnType<Parameters<typeof win.bind>[1]>>` で導出してキャストした。
  - deno.json: imports に `@std/http`（serveDir）追加、verify/check に `desktop/mod.ts main.ts` を、test に `desktop/` を、fmt/lint include に `desktop/` を追加。テスト: `desktop/bridge.test.ts`(6)＝ディスパッチ/undefined・null args/command 検証/args 検証/実 bindings 経由で read_directory が temp の a.zip を返す、`desktop/windowConfig.test.ts`(3)＝既定/保存値/部分指定。VERIFY グリーン（92 passed）。commit はこの ITERATE で。残（次タスク M4b）: ②ビューワー窓生成（`Deno.BrowserWindow` で `viewer.html?archive=...` を開く・`src/App.tsx` の `handleArchiveSelect` 相当）と**同一アーカイブ二重オープン防止**（`src/utils/windowLabel.ts` の `viewerLabel`/`hashCode` を desktop 側へ移植し開いている窓を label 管理）を `open_viewer` 等の binding として実装。その後 M3（webview の invoke shim 差し替え）→ M6（`deno desktop main.ts` ビルド）。GUI 表示の目視確認は人間に委ねる。

- [m5:paths] マイルストーン5で繰り返し「残」とされてきた **settings.json パス解決（appConfigDir 相当）** を `deno-app/backend/paths.ts` として実装。Tauri 版は `tauri-plugin-store` の `load("settings.json")` が app config dir（`app_config_dir`、identifier=`com.mekuri.reader`＝`src-tauri/tauri.conf.json` で確認）配下に保存していた。これを OS 別に再現: macOS=`$HOME/Library/Application Support/<id>`、Linux=`$XDG_CONFIG_HOME/<id>`（無ければ `$HOME/.config/<id>`）、Windows=`%APPDATA%\<id>`（Tauri v2 の `dirs::config_dir` + identifier 相当）。公開: `appConfigDir(env?)`/`settingsPath(env?)`/定数 `APP_IDENTIFIER`/`SETTINGS_FILE_NAME`/型 `PathEnv`。設計判断: `backend/` を Desktop 非依存かつテスト容易に保つため、OS（`Deno.build.os` 相当）と環境変数取得（`Deno.env.get` 相当）を `PathEnv` で注入可能にし、既定 `denoEnv()` は実ランタイム情報を使う（trash の deleter 注入と同じ方針）。区切り文字は OS で切替（windows=`\\`）。`paths.test.ts`(8) 追加（mac/linux/win・XDG fallback・空文字 XDG・必須 env 欠落で throw）、`mod.ts` から re-export。VERIFY グリーン（83 passed）。commit 0916b65。これでアプリ層は `Store.load(settingsPath(), {autoSave:true})` で Tauri 版と同一パスを開ける。残: 実際にアプリ層（マイルストーン4/6・canary）でこのパスを Store に渡して favorites/settings を配線する処理。

- [m3:trash] バックエンド/バインディング未移植だった `trash_file` を移植。`deno-app/backend/fs.ts` に `trashFile(path, deleter?)` を追加（`src-tauri/src/commands/fs.rs` の `trash_file` 相当）。検証ロジック（存在＝`Deno.stat`、通常ファイル判定、Rust と同じエラーメッセージ `File does not exist:` / `Path is not a file:`）は純粋に保ち、実際のゴミ箱移動はネイティブ依存になるため注入可能な `FileDeleter` 型に分離。既定 deleter は `npm:trash@^9`（`deno.json` の imports に追加、`deno eval` で Deno 動作確認済み＝subprocess/ネイティブ依存だが import 可）を**動的 import**（`await import("trash")`）し、検証パスのテストでは mock deleter を注入するので実トラッシュ依存はテスト時にロードされない。`bindings/invoke.ts` に `case "trash_file"` を追加（引数 `path`、これでフロント `src/api/directory.ts` の `trashFile` が配線可能に＝以前 "Unknown command" だった穴を解消）。`mod.ts` から `trashFile`/`FileDeleter` を re-export。テスト: `fs.test.ts` に3件（存在しない/ディレクトリは deleter 呼ばず reject、通常ファイルは deleter にパスを渡し成功）、`invoke.test.ts` に1件（trash_file ディスパッチ＝検証エラー透過）追加。VERIFY グリーン（75 passed）。commit 5fa90e1。設計判断: 実トラッシュは副作用＝アプリ層で `trashFile(path)`（既定 deleter）を使い、テストでは注入。残: マイルストーン3 の `src/api/` 配線替え本体は依然 canary/IPC ブロック（[m3:blocked] 参照）だが、バインディング層は trash 含め全コマンド対応済みになった。

- [m5:settings] マイルストーン5の高レベルラッパ `deno-app/backend/settings.ts` を新設（`src/api/favorites.ts` + `src/api/settings.ts` 相当）。お気に入り（`getFavorites`/`addFavorite`/`removeFavorite`）とウィンドウ/ビューワー設定（`getWindowSettings`/`saveWindowSettings`/`getViewerSettings`/`saveViewerSettings`）を `Store`（backend/store.ts）ベースで実装。型 `MainWindowSettings`/`ViewerWindowSettings`/`ViewMode`/`ReadingDirection`、キー定数（`FAVORITES_KEY` 等）、既定値（`DEFAULT_MAIN_SETTINGS`={1000,700,300}/`DEFAULT_VIEWER_SETTINGS`={1200,900,spread}、`src/utils/constants.ts` 相当）も定義。設計判断: `backend/` を Desktop 非依存に保つため `src/api/store.ts` の `getStore()`（内部で settings.json パス解決＝tauri-plugin-store の `load`）は移植せず、`Store` インスタンスを各関数の第1引数で受け取る形にした。アプリ層が `Store.load(settingsPath, {autoSave:true})` で開いて渡せば Tauri 版同様 `set` ごとに自動保存される。get 系は `Store.get` が同期のため同期関数（React 版は plugin-store が非同期で await していたが backend は同期で良い）。`settings.test.ts`(11) 追加、`backend/mod.ts` から re-export。VERIFY グリーン（71 tests passed）。commit 0090ea2。残: 実際の settings.json パス解決（appConfigDir 相当）とアプリ層からの呼び出し配線はマイルストーン4/6（canary）で。

- [m5:store] マイルストーン5の中核 `deno-app/backend/store.ts` を新設（Tauri 版 `tauri-plugin-store` 相当の JSON ファイル永続化ストア）。`Store.load(path, {defaults, autoSave})` で開き、`get<T>(key)`/`has`/`keys`/`entries`（同期・メモリ参照）、`set`/`delete`/`clear`/`save`（非同期・ファイル IO）を提供。フロントが `src/api/store.ts` の `load(name,{defaults,autoSave})` → `get`/`set` で使っていた挙動に合わせた（`favorites.ts`/`settings.ts` が依存）。設計判断: `backend/` を Desktop 非依存に保つため永続化先パスは呼び出し側（アプリ層）が与える形にし、テストは `Deno.makeTempDir` の一時ファイルで完結。ファイル欠落時は defaults を初期状態にするがファイルは書かない（Tauri 版同様）。`store.test.ts`(10) 追加、`backend/mod.ts` から re-export。VERIFY グリーン（60 tests passed）。commit 751048e。残: 実際の settings.json パス解決（appConfigDir 相当）と favorites/settings/windowSettings/viewerSettings のラッパ配線はアプリ層（マイルストーン4/5）で。

- [m3:bindings] マイルストーン3の基盤として `deno-app/bindings/invoke.ts`（+ `bindings/mod.ts` 公開, `invoke.test.ts` 8件）を新設。Tauri 版フロントが `@tauri-apps/api/core` の `invoke(command, args)` で呼んでいた Rust コマンドを、プロセス内で `backend/mod.ts` の関数へディスパッチする invoke 互換レイヤ。コマンド名→backend 対応: `read_directory`→readDirectory(path) / `search_directory`→searchDirectory(path,query) / `read_file_base64`→readFileBase64(path) / `list_archive_images`→listImages(archivePath) / `get_archive_image`→getImageBase64(archivePath,entryName) / `analyze_archive_contents`→analyzeContents(archivePath) / `extract_nested_archive`→extractNestedArchive(parentPath,nestedName)。引数名は React 側が現状渡している camelCase（archivePath 等）をそのまま受ける。未知コマンド/欠落引数はエラー。`trash_file` はネイティブ trash 未移植のため意図的に未対応（Unknown command で reject、マイルストーン3/別途）。`deno.json` の verify/check/test タスクを `bindings/mod.ts`・`bindings/` も対象に拡張（これまで bindings は fmt/lint のみで型検査・テスト対象外だった）。落とし穴: invoke は async にしないと `requireString` の同期 throw が reject にならず assertRejects が「throw when expected to reject」で落ちる（mod.ts と同じ）。さらに async 化すると lint require-await のため各 return を `await fn() as T` 形にする必要あり（`as Promise<T>` 直返しは await 無しで弾かれる）。VERIFY グリーン（51 tests passed）。commit 77ada55。次周は実際に `src/api/` を1ファイルずつこのバインディングへ配線替え。

- [m2:mod] `deno-app/backend/archive/mod.ts` を `src-tauri/src/archive/mod.rs` から移植（形式ディスパッチ）。`detectFormat`（拡張子 zip/cbz→zip, rar/cbr→rar、それ以外は `Unsupported archive format: .ext` エラー）で `listImages`/`analyzeContents`/`extractNestedArchive`/`getImageBase64` を zip.ts/rar.ts へ振り分ける統一 API を実装。`ArchiveContents` 型は zip.ts の定義を re-export（移動はせず import 共有を継続）。`backend/mod.ts` を `export * as rar` の暫定公開から統一ディスパッチ API（`./archive/mod.ts` 由来）へ整理。テストは mod.rs の `test_unsupported_archive_format`/`test_nonexistent_*`/`test_cbr_dispatches_to_rar`/`test_rar_get_image_nonexistent` を移植（`mod.test.ts` 9件）。落とし穴: 各公開関数を `async` にして `detectFormat` の同期 throw を必ず reject に変換すること（非 async だと `assertRejects` が「throw when expected to reject」で落ちる）。VERIFY グリーン（43 tests passed）。commit 0f0bc0f。これでマイルストーン2（バックエンドロジック移植）は実質完了（画像 Base64 data URL は zip/rar/mod とも実装済み）。

- [m2:rar] `deno-app/backend/archive/rar.ts` を `src-tauri/src/archive/rar.rs` から移植（`listImages` / `analyzeContents` / `extractNestedArchive` / `getImageBase64`）。RAR 読み取りに `npm:node-unrar-js@^2.0.2`（解決版 2.0.2）を採用＝Deno で動作する wasm 実装の unrar で、`backend/` を Desktop 非依存に保てるため（純粋 JS RAR ライターが無いのが理由で書き込みは不可）。`createExtractorFromData({data})` の `data` は `ArrayBuffer` 必須なので `new Uint8Array(Deno.readFile結果).buffer` で新規確保して渡す（`data.buffer` 直渡しは `ArrayBufferLike`→TS2322）。`getFileList().fileHeaders`（列挙）/ `extract({files:[name]}).files`（抽出）を使用。`ArchiveContents` 型は `zip.ts` から import 共有し re-export。`mod.ts` では zip と同名関数のため `export * as rar` で名前空間公開（形式ディスパッチ=archive/mod.ts は別タスク）。テスト戦略は元 Rust（mod.rs）と同じくエラーパス中心（RAR フィクスチャ生成不可のため）＝存在しないファイル/不正データでのエラーを検証。`rar.test.ts`(6) 追加。VERIFY グリーン（34 tests passed）。commit 878cc58。
- [m2:zip] `deno-app/backend/archive/zip.ts` を `src-tauri/src/archive/zip.rs` から移植（`listImages` / `analyzeContents` / `extractNestedArchive` / `getImageBase64`）。`ArchiveContents` 判別共用体（`{type:"Images"|"NestedArchives"|"Empty"; names?}`、Rust の serde tag="type" と一致）も定義。ZIP 読み書きライブラリに `jsr:@zip-js/zip-js@^2.7`（解決版 2.8.26）を採用＝Deno 対応・純粋 JS で `backend/` を Desktop 非依存に保て、テスト用 ZIP 生成（ZipWriter）も同一 lib でできるため。`configure({useWebWorkers:false})` でプロセス内実行（worker リーク回避）。`zip.test.ts`(10) 追加、`mod.ts` から re-export。VERIFY グリーン（28 tests passed）。commit 26477c8。落とし穴: zip-js の `Entry` は file/`DirectoryEntry` の union なので `getData` 参照前に `entry.directory` で絞る必要がある。
- [m2:fs] `deno-app/backend/fs.ts` を `src-tauri/src/commands/fs.rs` から移植（`readDirectory` / `searchDirectory` / `readFileBase64`）。依存する拡張子判定を `backend/extensions.ts`（`isArchiveFile`/`isPdfFile`/`isImageFile`/`mimeTypeFromName`、archive/mod.rs 相当）に切り出し。`fs.test.ts`(9)・`extensions.test.ts`(4) 追加、`mod.ts` から re-export。VERIFY グリーン（18 tests passed）。commit bcd0f22。`DirectoryEntry` は React 互換のため snake_case フィールド維持。`trash_file` はネイティブ trash 依存のため未移植（マイルストーン3/別途検討）。
- [setup] スキャフォールド作成。`deno-app/deno.json`（verify タスク）、`deno-app/backend/sort.ts`（自然順ソートの参照実装）+ `sort.test.ts`、`deno-app/backend/mod.ts`、`bindings/`・`archive/` ディレクトリを用意。`cd deno-app && deno task verify` がグリーンであることを確認済み。マイルストーン1完了・マイルストーン2の sort.ts 完了。

## Open（未解決 / 次周への申し送り）

- **[operator/m8] 2026-06-27: M7 完了（白画面解消＝bind→HTTP transport へ全面移行、`open_viewer` も HTTP 化済み）。`deno task smoke` は**メイン窓＋ビューワー窓の描画を検証する形に拡張済み**（make-test-cbz でテスト CBZ 生成→serve ポート解析→`open_viewer` を HTTP で叩く）で現状グリーン。ユーザー実機確認で残2課題＝VISION の M8 を実施する。
  - **8a 画像取得の効率化 → 完了（[m8a:cache]）**: `backend/archive/cache.ts`（path 別 LRU=2 の `Promise<Uint8Array>` キャッシュ）を新設し `openZip`/`openRar` を `readArchiveBytes` 経由へ。連続/並行の画像取得で書庫 `Deno.readFile` が 1 回に収まることをスパイで回帰検証済み（`cache.test.ts` 5 + `zip.test.ts` 統合 2）。`deno task verify` 223 passed。詳細は Done [m8a:cache]。
  - **8b ビューワー右クリックメニュー**: `menu_` だけ未だ bindings transport で不達。クリック結果を main→webview へ返す push が `executeJs`/`win.bind` と同じく採用窓に届かないのが核心。`Deno.serve` に SSE/long-poll を足して webview が購読し、`handleEventCommand` の窓間イベント delivery と menu クリック配送をそこへ移す（`executeJs` push 廃止）→ `menu_` を HTTP 化し `frontend/invoke.ts` の `BINDINGS_SCOPED_PREFIXES` を空に。
    - **基盤 → 完了（[m8b:pushhub]）**: `desktop/pushHub.ts`（`PushHub`＝subscribe/broadcast/sendTo、`handleEventsRequest`＝`GET /__events?windowLabel=` の SSE、`formatSse`、チャネル定数）を純粋・テスト可能に新設（`pushHub.test.ts` 8、`deno task verify` 231 passed）。残りは配線＝下記の順で1周1ステップ。
    - **次周（最優先・1タスク）**: main.ts で `PushHub` 生成＋`Deno.serve` に `GET /__events` を配線し、`handleEventCommand` の窓間イベント delivery を `executeJs` push → `hub.broadcast({channel:EVENT_CHANNEL,...})` へ移す（event は全窓ブロードキャストで label 不要＝最も素直）。続けて menu クリック配送を `hub.sendTo(windowLabel,...)` へ（`contextMenuClick` の窓 label 解決が要る）→ `menu_` HTTP 化 → `frontend` に EventSource 購読 shim（`__mekuriDeliverEvent`/`__mekuriMenuClick` へ配送）。
  - 1周1テーマ（8a→8b 推奨）。**LOOP_DONE は verify＋check-no-tauri＋pnpm test＋smoke 全緑＋ M8 の回帰テスト緑から**。`main.ts` の ERR_FORWARDER（index.html/viewer.html 両方へ注入）・`deno-app/scripts/`（make-test-cbz・拡張 smoke・check-no-tauri）は消さないこと。**
- **[m7:http-window → 完了] window 系統の HTTP 化は [m7:http-window] で実装済み**＝`window_*` は `fetch("/__invoke")` に自窓 label を載せて送り、main 側 `resolveWindow` が label→窓を引いて `handleWindowCommandByLabel`→`handleWindowCommand` へ委譲。data/store/window が HTTP、残るは event/menu のみ bindings。`deno task verify` 216 passed。**次周（最優先・1タスク）**: (A) **要 operator/canary**: `deno task smoke` を実行し、起動時の window 系 reject（`window_set_size`/`window_show`/`window_set_title`）が消えたか観測（残る reject は event/menu 由来のはず）。(B) 残る **event→menu を HTTP 化**（白画面の完全解消＝`win.bind` 撤廃）。event は全窓ブロードキャスト（`handleEventCommand(liveWindows,...)`）で label 不要＝HTTP 化は素直。menu は `handleMenuCommand(win,...)` が窓固有なので window と同じく windowLabel→窓解決が要る（`handleWindowCommandByLabel` と同型の menu 版を作るか汎用化）。1周1系統（event→menu）。これが完了すれば smoke 緑＝白画面解消で LOOP_DONE ゲートへ。詳細は Done の [m7:http-window]/[m7:http-transport-step1]/[m7:denidian-http-pivot] 参照。**

- **[m7:http-transport-step1 → 第一段完了] [m7:denidian-http-pivot] の HTTP 移行の第一歩は [m7:http-transport-step1] で実装済み**＝窓非依存の data/store 系は `fetch("/__invoke")` 経由（`main.ts` の `Deno.serve` に POST /__invoke、`desktop/httpInvoke.ts` が純粋ロジック）。window は [m7:http-window] で HTTP 化済み。event/menu は `bindings.invoke` のまま（上記参照）。

- **[operator/smoke-msg] 2026-06-26（→ 真因確定済み）: 実メッセージは `Error: No callback bound for: invoke`（×3）。当初仮説（起動時 invoke の unhandled reject＝bind 前 await レース等）は [m7:whitescreen-rootcause] の計測で**否定**され、真因は「表示 webview と bind 済み `win` が別窓」と確定した。以下は履歴。**白画面の真因は `__TAURI_INTERNALS__` ではなく、**起動直後に走る `bindings.invoke(...)` の時点で main 側の `win.bind("invoke", ...)` がまだ登録されていない**こと（Deno Desktop が "No callback bound for: <name>" を返す）。診断リード: (1) `main.ts` は `await Store.load(...)`（非同期）の**後**に `new Deno.BrowserWindow()`＋`bindInvoke(win)` しており、その await の間に**自動オープンされた初期ウィンドウが先にページを読み込み invoke を呼ぶ**レースの疑い → 対策: bind を全ての await より前・ページ読み込み前に行い、`store` はハンドラ内で遅延 `await` する（`bindInvoke` を window 生成直後に呼ぶ）。(2) `new Deno.BrowserWindow(opts)` が自動初期ウィンドウを**採用**しているか要確認（別窓を作っていると bind した窓と表示窓が別になり "No callback bound" になる）。Deno Desktop docs の windows ページや `bindingsReady` イベントも参照。(3) フロント shim（`frontend/invoke.ts`）側で bindings 準備待ち（数十 ms リトライ/キュー）を入れる保険も可。**次周タスク（最優先・1タスク）**: この invoke バインディング準備タイミングを直し、`cd deno-app && deno task smoke` が `[web]` 行ゼロで通ることを確認する。通れば [m7:final-gate]② 達成。残りは ③`pnpm test`(root、実体は `npx vitest run src/` で既に緑) のみ → 全 DoD 達成で `LOOP_DONE`。**
- **[m7:dialog-open-blocked → 解決] 旧ブロック item は [m7:dialog-open] で解決済み**。代替案①（`Deno.Command` で OS ネイティブ picker を起動する `open_folder` バインディング）を実装し `check-no-tauri` 0件に到達。詳細は Done の [m7:dialog-open] 参照。**残るは LOOP_DONE ゲート＝package.json の @tauri-apps 依存除去＋smoke＋pnpm test のみ**（下記 [m7:final-gate]）。
- **[m7:final-gate] LOOP_DONE まで残るタスク**: ①`package.json` の `@tauri-apps/*` 依存除去は **[m7:final-gate-deps] で完了**。②`cd deno-app && deno task smoke` は **[m7:smoke-instrument] で初実行＝まだ FAIL**（`[web] reject: @user-script:1:58:33` ×3＝白画面未解消。起動時の `bindings.invoke`(store_get/window_set_size/window_show/window_set_title) 系が unhandled reject の疑い）。同周で ERR_FORWARDER をエラー詳細出力へ強化済み。**次周タスク（最優先）**: 再 smoke で `[web] reject:` の**実メッセージ**を取得し、どの invoke コマンドが/なぜ reject するか特定→真因修正（候補: bind ハンドラ throw、handleWindowCommand/handleStoreCommand の引数不一致、bindings.invoke の戻り値型）。③リポジトリルートで `pnpm test`（ルートに vitest 設定が無く deno-app の Deno テストを拾って失敗し得る＝[m3:rewire-invoke] 落とし穴。DoD が指す `pnpm test` の実体は要確認＝おそらく `npx vitest run src/` 相当＝既に緑）。②③が緑なら全 DoD 達成＝`LOOP_DONE`。

- **[operator/m7-whitescreen] 2026-06-26: ⚠ 以前の [ALL-DONE]「完了」宣言は誤りだった（撤回）。`deno desktop` で起動すると**白画面**になる。原因 = フロント `src/` が `src/api/` 以外で `@tauri-apps/*` を直接使用（window/dialog/event/menu）し、Deno Desktop の webview に `__TAURI_INTERNALS__` が無く起動時クラッシュ。M6 の「アプリが実際に描画される」は**未達**。→ **VISION の M7 を最優先で実施**。残存箇所は `cd deno-app && deno task check-no-tauri` で常時列挙できる（**window=[m7:window]・webviewWindow=[m7:webviewwindow]・dialog ask=[m7:dialog-ask]・event=[m7:event]・menu=[m7:menu]・dialog open=[m7:dialog-open] で全系統移行済み＝現状0件 OK**）。残るは LOOP_DONE ゲートのみ（[m7:final-gate]＝package.json の @tauri-apps 依存除去＋smoke＋pnpm test）。方針: `main.ts` に必要なバインディング（window のサイズ取得/設定・resize 通知、native dialog、窓間 event の pub/sub、menu）を足し、`deno-app/frontend/` に browser-safe な shim（`frontend/invoke.ts` と同様に `globalThis.bindings.*` を呼ぶ）を用意して `src/` から呼ぶ。ビューワー窓生成は既存 `open_viewer` を使う。1周1系統（window→webviewWindow→dialog→event→menu）を目安、各周 `deno task verify` 緑でコミット。**「ビルド成功」≠「完了」。LOOP_DONE は `deno task verify` ＋ `deno task check-no-tauri` ＋ `deno task smoke` ＋（リポジトリルートで）`pnpm test` が全部グリーンになってから**。最後に `package.json` の `@tauri-apps/*` 依存も削除。挙動は Tauri 版（`src-tauri/` と元 `src/`）と等価に保ち機能を削らない。`main.ts` の ERR_FORWARDER と `deno-app/scripts/`（ゲート）は消さないこと。以下の古い注記（[ALL-DONE] 撤回済・[next-m6]・canary note の「ビルド成功で LOOP_DONE 可」）は無効。**
- **[operator/canary-ready] 2026-06-26: canary deno 2.9.0（`deno desktop` サブコマンド入り）を `~/.deno-canary/bin` に導入済みで、本走では PATH 先頭に置いてある。DISCOVER で `deno --version`（`2.9.0...canary`）と `deno desktop --help`（`Build and run desktop applications` が出る）を確認できる。よって M4・M6 のブロックは解除。`[m3:blocked]` の分析どおり次の順で進めること: ①M4 = `deno-app/main.ts` で `Deno.BrowserWindow` を起動し、webview↔Deno メイン間の IPC ブリッジ（メイン側で `bindings/invoke.ts` を呼ぶ受け口 + webview へ結果を返す経路）を確立 **← [m4:main] で完了（main.ts + desktop/bridge.ts + windowConfig.ts）。残るのは M4b＝ビューワー窓生成 + 二重オープン防止** → ②M3 = webview 側に browser-safe な invoke shim（`backend/` を直接 import せず IPC でメインへ送る）を置き、`src/api/*.ts` の `@tauri-apps/api/core` import をそれへ差し替え（`src/components/` は無改変） → ③M6 = `deno desktop deno-app/main.ts` でビルド。VERIFY は `deno task verify` に `deno check main.ts`（および IPC 関連ファイル）を足して main.ts も型検査対象にすること（canary の `Deno.BrowserWindow` 型を使うため）。注意: M6 の「ウィンドウが実際に表示される」確認は GUI のため自動検証困難＝**ビルド成功までを VERIFY で担保し、目視起動確認は人間に委ねる**旨を Open に残すこと（その状態なら他 DoD が揃えば `LOOP_DONE` を出してよい）。**
- [m3:blocked] マイルストーン3（`src/api/` 配線替え）は現状ブロックと判断（この周は安全側に倒し未着手）。理由: `src/` は Vite/tsc でビルドされ webview（ブラウザ）で動くコード。`deno-app/backend/` は `npm:node-unrar-js`(wasm) や `Deno.readFile`/`Deno.makeTempDir` 等の Deno/ネイティブ依存を持つため、`src/api/*.ts` から `deno-app/bindings`（→`backend/mod.ts`）を直接 import するとフロントのビルドが壊れる。これは Tauri の IPC に相当する webview↔Deno メイン間ブリッジ（`Deno.BrowserWindow` のメッセージ機構 = canary 必須＝マイルストーン4）に実質依存する。加えて `src/` の変更は `deno task verify` の検証対象外で closed loop で品質確認できない。→ マイルストーン3は canary 導入＆マイルストーン4の IPC 機構確定後に、(a) webview 側に置く browser-safe な invoke shim（backend を直接 import しない・IPC でメインへ送る）を用意し、(b) `src/api/*.ts` の `@tauri-apps/api/core` import をそれへ差し替える、という順で着手する。それまでは backend ロジック（2）と設定永続化（5）を先に進める（[m5:store] で着手済み）。
- **[next-m6] マイルストーン3 は [m3:store-swap] で完全完了。残るマイルストーンは M6 のみ**＝`deno desktop deno-app/main.ts` でビルドでき、アプリが起動すること（canary 2.9.0 導入済み・`deno desktop --help` 確認済み）。アプローチ: `cd deno-app && deno desktop main.ts` を試す（または `--output` でアプリ生成）。main.ts は `Deno.serve`+`serveDir` で `../dist/` 配信＋`Deno.BrowserWindow` 起動なので、事前に `pnpm build`（リポジトリルート）で `dist/` を生成しておく必要がある。注意（operator note）: ウィンドウ実表示は GUI で自動検証困難＝**ビルド成功までを VERIFY で担保し、目視起動確認は人間に委ねる**。ビルドが通れば他 DoD は全て揃っているので `LOOP_DONE` を出してよい。VERIFY は `deno task verify`（既に main.ts を `deno check` 対象に含む）に加え、M6 では `deno desktop` のビルドが成否の判定。`deno desktop` がウィンドウを開いて待機（ブロッキング）する可能性があるため、ビルドのみ確認したい場合は `--output` でアプリバンドル生成を狙うか、タイムアウト/バックグラウンド実行を検討する。

- [next] （履歴）マイルストーン3: `directory.ts`/`archive.ts` の invoke 配線替えは **[m3:rewire-invoke] で完了**（`src/api/invoke.ts` 経由で `deno-app/frontend/invoke.ts` を `../../deno-app/...` 相対 `.ts` import、Vite alias 不要）。残る M3 は `favorites.ts`/`settings.ts`/`store.ts` の 3 本だが、これらは `invoke` ではなく `@tauri-apps/plugin-store`（`load("settings.json")` → `get`/`set`、autoSave）に依存している。Deno 側は M5 で `backend/store.ts`（plugin-store 相当）・`settings.ts`（favorites/window/viewer ラッパ）・`paths.ts`（settings.json パス解決）を実装済みだが、フロント（webview）から直接 `backend/store.ts` を import するとファイル IO（`Deno.writeTextFile` 等）でブラウザビルドが壊れる。よって store 系の配線は invoke と同様に **アプリ層（`main.ts`）で Store を開き、`win.bind` で store 操作（get/set/save）を webview へ公開 → webview 側 shim → `src/api/store.ts` 差し替え**という IPC 経由の追加配線が必要（M5 アプリ層配線とセット）。次タスク候補: (A) M6 = `deno desktop main.ts` のビルド成功確認（canary 導入済み、operator note の手順③）、(B) store 系 IPC 配線の最終段③＝`src/api/store.ts` の `getStore` 内 `import { load } from "@tauri-apps/plugin-store"` を webview shim（`deno-app/frontend/store.ts` の `load`）へ `../../deno-app/frontend/store.ts` 相対 `.ts` import で差し替え（②`main.ts` 合流配線は [m3:store-wire] で完了済み。①shim/②dispatcher/②main 合流まで全て済み、残るは③のみ）。③は `src/` 側のため `deno task verify` の対象外＝フロント検証は `npx tsc --noEmit` / `npx vitest run src/`（`pnpm test` はルートに vitest 設定が無く deno-app の Deno テストを拾って失敗するので使わない）/ `pnpm lint` / `pnpm build`。`src/components/` は無改変（RULES）。
- [next2] zip.rs にあった `extract_nested_archive` は Tauri 版では temp dir をアプリ終了まで保持（TEMP_DIRS）。zip.ts では `Deno.makeTempDir` で作るだけにし後始末はアプリ側へ委ねた。マイルストーン4/5 でアプリ層を作る際に temp ディレクトリのライフサイクル管理（終了時クリーンアップ）を実装すること。

## Notes（学び / 落とし穴）

- 作業ディレクトリ（cwd）はリポジトリルート。ループ用ドキュメントは `loop/` 配下。VERIFY は `cd deno-app && deno task verify`。
- マイルストーン4（`Deno.BrowserWindow`）・6（`deno desktop`）は canary（deno 2.9.0+）が必要。stable 2.7.12 では型検査・起動できないため、これらは canary 導入後に着手する。それまでは backend ロジック（2）とフロント配線（3）・設定永続化（5）を進める。
- `backend/` は Deno Desktop API に依存させない（単体テスト可能に保つ）。公開 API は `backend/mod.ts` から re-export し、`deno check backend/mod.ts` で型検査される状態を維持する。
- 正解は元の Tauri 実装（`src-tauri/`）と React フロント（`src/`）の挙動。移植時は対応する Rust ファイルを読んで仕様を合わせる。
